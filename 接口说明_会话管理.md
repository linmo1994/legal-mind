# /api/sessions/{session_id} 接口说明

## 接口概述

`/api/sessions/{session_id}` 是一个会话管理接口，根据HTTP方法的不同，提供不同的功能。

## 支持的HTTP方法

### 1. GET /api/sessions/{session_id}
**作用：** 获取指定会话的详情

**请求：**
- 方法：GET
- 路径：`/api/sessions/{session_id}`
- 无需请求体

**响应：**
- 状态码：200（成功）或 404（会话不存在）
- 响应体：会话对象（JSON格式）
  ```json
  {
    "session_id": "sess_xxx",
    "title": "会话标题",
    "status": "active",
    "current_intent": "intent_name",
    "collected_parameters": {},
    "missing_parameters": [],
    "stage": "idle",
    "context_cache": {},
    "created_at": "2024-01-01T00:00:00",
    "updated_at": "2024-01-01T00:00:00",
    "last_user_input": "用户最后输入的问题"
  }
  ```

### 2. POST /api/sessions/{session_id}
**作用：** 更新指定会话的信息

**请求：**
- 方法：POST
- 路径：`/api/sessions/{session_id}`
- 请求体（JSON）：
  ```json
  {
    "status": "active",
    "current_intent": "intent_name",
    "collected_parameters": {},
    "missing_parameters": [],
    "stage": "idle",
    "context_cache": {}
  }
  ```

**响应：**
- 状态码：200
- 响应体：更新后的会话对象

**客户端调用位置：**
- `saveSession()` 函数在每次对话结束后调用
- 用于保存会话状态（状态、意图、参数、阶段等）

### 3. DELETE /api/sessions/{session_id}
**作用：** 删除指定会话

**请求：**
- 方法：DELETE
- 路径：`/api/sessions/{session_id}`

**响应：**
- 状态码：200（成功）或 404（会话不存在）
- 响应体：`{"success": true}`

## 客户端使用场景

1. **保存会话状态**（`saveSession`函数）
   - 在 `handleUserInput` 的 `finally` 块中调用
   - 每次对话结束后自动保存会话状态
   - 更新会话的 `status`、`current_intent`、`collected_parameters`、`missing_parameters`、`stage`、`context_cache` 等字段

2. **加载会话详情**（`loadSession`函数）
   - 从会话列表中选择会话时调用
   - 获取会话的完整信息，包括历史消息

3. **删除会话**
   - 用户主动删除会话时调用

## 数据库存储

会话数据存储在SQLite数据库中（`sessions.db`），包括：
- 会话基本信息（session_id、title、status等）
- 会话状态（current_intent、collected_parameters、missing_parameters、stage）
- 上下文缓存（context_cache）
- 时间戳（created_at、updated_at）
- 最后用户输入（last_user_input）

消息数据存储在 `messages` 表中，通过 `session_id` 关联。
