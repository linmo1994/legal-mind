# 提示词模板调用方案分析

## 问题回顾

当调用 `judge_work_guide` 提示词模板后，LLM 又返回了调用提示词模板的指令，导致死循环。

## 当前方案的问题

**当前方案**：在运行时修改 `system_prompt`，移除关于调用提示词模板的指令。

**问题**：
1. ❌ `system_prompt` 应该是固定的配置，不应该在运行时动态修改
2. ❌ 使用正则表达式修改 `system_prompt` 容易出错，维护困难
3. ❌ 破坏了配置的完整性和可读性
4. ❌ 如果 `system_prompt` 格式变化，正则表达式可能失效

## 更合理的方案

### 方案1：在提示词模板内容前添加说明（推荐）

**思路**：保持 `system_prompt` 不变，在提示词模板内容前添加明确的说明。

**优点**：
- ✅ 保持 `system_prompt` 的固定性
- ✅ 不需要修改配置
- ✅ 逻辑清晰，易于维护
- ✅ 提示词模板本身可以包含"这是已调用的模板"的说明

**实现**：
```python
# 在提示词模板内容前添加说明
prompt_instruction = f"""【重要：提示词模板已调用】
提示词模板 "{prompt_name}" 已经调用，你现在应该直接按照以下提示词模板的内容工作，**不要再返回调用提示词模板的指令**。

请直接按照提示词模板中的工作流程执行任务，输出最终结果，而不是再次返回 invoke_tool_or_resource 或 invoke_prompt 的指令。

--- 提示词模板内容 ---
{prompt_text}

--- 系统提示（保持不变） ---
{system_prompt}"""
```

### 方案2：将提示词模板作为独立的系统消息

**思路**：保持原始 `system_prompt` 不变，将提示词模板内容作为额外的系统消息。

**优点**：
- ✅ 完全保持 `system_prompt` 不变
- ✅ 提示词模板内容独立，优先级更高
- ✅ 符合多轮对话的消息结构

**实现**：
```python
# 保持原始 system_prompt 不变
messages.append({"role": "system", "content": system_prompt})

# 添加提示词模板作为额外的系统消息
if prompt_text:
    prompt_system_message = f"""【提示词模板已调用】
提示词模板 "{prompt_name}" 已经调用，请直接按照以下内容工作，不要再返回调用提示词模板的指令。

{prompt_text}"""
    messages.append({"role": "system", "content": prompt_system_message})
```

### 方案3：在用户消息上下文中添加说明

**思路**：保持 `system_prompt` 不变，在用户消息前添加上下文说明。

**优点**：
- ✅ 保持 `system_prompt` 不变
- ✅ 上下文信息清晰

**缺点**：
- ⚠️ 用户消息可能过长
- ⚠️ 说明的优先级可能不如系统消息高

## 推荐方案

**推荐使用方案1**：在提示词模板内容前添加说明，同时保持 `system_prompt` 不变。

**理由**：
1. 保持配置的固定性
2. 逻辑清晰，易于理解和维护
3. 提示词模板内容在前，优先级更高
4. 不需要修改消息结构

## 实现建议

修改服务端代码，保持 `system_prompt` 不变，只在提示词模板内容前添加说明：

```python
if prompt_text:
    # 保持 system_prompt 不变
    # 在提示词模板内容前添加说明
    prompt_instruction = f"""【重要：提示词模板已调用】
提示词模板 "{prompt_name}" 已经调用，你现在应该直接按照以下提示词模板的内容工作，**不要再返回调用提示词模板的指令**。

请直接按照提示词模板中的工作流程执行任务，输出最终结果，而不是再次返回 invoke_tool_or_resource 或 invoke_prompt 的指令。

--- 提示词模板内容 ---
{prompt_text}

--- 系统提示 ---
{system_prompt}"""
    
    system_prompt = prompt_instruction  # 这里只是组合，不是修改原始配置
```

这样既解决了死循环问题，又保持了配置的固定性。
