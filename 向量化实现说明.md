# 向量化实现逻辑说明

## 1. 向量维度

### 当前实现
- **向量维度：384维**
- **模型：** `paraphrase-multilingual-MiniLM-L12-v2`
- **模型特点：**
  - 多语言支持（包括中文）
  - 轻量级模型，速度快
  - 输出384维向量

### 代码位置
```python
# server/vector_service.py 第45行
self.model = SentenceTransformer('paraphrase-multilingual-MiniLM-L12-v2', device='cpu')

# 第86行：生成嵌入
embedding = self.model.encode(text, convert_to_numpy=True)
# 返回384维向量
```

### 验证结果
实际测试显示向量维度为 **384维**。

---

## 2. 文本分块策略

### 当前实现：启发式分块（非纯语义分块）

**分块方式：** 基于字符数的均等切割 + 智能分割点查找

### 分块参数
- **块大小（chunk_size）：** 500字符（默认）
- **重叠大小（chunk_overlap）：** 50字符（默认）
- **分割策略：** 尝试在句号、换行符等位置分割

### 分块逻辑（代码第89-125行）

```python
def _split_text(self, text: str, chunk_size: int = 500, chunk_overlap: int = 50):
    """
    分块流程：
    1. 如果文本长度 <= chunk_size，直接返回整个文本
    2. 否则，按以下步骤分割：
       a. 从位置start开始，取chunk_size个字符
       b. 尝试在块内查找最后一个句号（。）或换行符（\n）
       c. 如果找到的分割点位置 > chunk_size * 0.5（即不在前半部分），
          则在该位置分割，否则保持原chunk_size
       d. 下一个块的起始位置 = 当前块结束位置 - chunk_overlap（重叠）
    """
```

### 分块特点

#### ✅ 优点
1. **保持语义完整性**：尽量在句号、换行符处分割，避免截断句子
2. **重叠机制**：50字符重叠，确保上下文连续性
3. **简单高效**：不需要复杂的NLP处理，速度快

#### ⚠️ 局限性
1. **非纯语义分块**：不是基于语义单元（如段落、主题）分割
2. **固定大小**：主要基于字符数，可能截断长句子
3. **简单启发式**：只查找句号和换行符，不处理其他标点

### 分块示例

假设有以下文本（600字符）：
```
"第一条 为了规范合同行为，保护合同当事人的合法权益...（500字符后）...第二十条 合同争议的解决方式包括协商、调解、仲裁和诉讼。"
```

**分块结果：**
- **块1（0-500字符）**：在句号处分割（如果找到）
- **块2（450-950字符）**：从450开始（50字符重叠），包含"第二十条"的内容

---

## 3. 向量生成流程

### 完整流程

```
文档输入
  ↓
文本分块（_split_text）
  ├─ 块1: "第一条 为了规范..."
  ├─ 块2: "第二十条 合同争议..."
  └─ 块N: "..."
  ↓
为每个块生成向量（_generate_embedding）
  ├─ 块1 → 384维向量
  ├─ 块2 → 384维向量
  └─ 块N → 384维向量
  ↓
存储到ChromaDB
  ├─ ID: document_id_chunk_0
  ├─ 向量: [0.123, 0.456, ...] (384维)
  ├─ 文本: "第一条 为了规范..."
  └─ 元数据: {document_id, chunk_index, total_chunks, ...}
```

### 向量生成方法

1. **正常情况**：使用SentenceTransformer模型生成384维向量
2. **备用方案**：如果模型加载失败，使用MD5哈希生成384维向量（仅用于测试）

---

## 4. 改进建议

### 当前实现的改进空间

#### 1. 语义分块（可选增强）
可以改进为基于语义的分块：
- 使用NLP工具识别段落边界
- 基于主题分割
- 使用更智能的句子边界检测

#### 2. 可配置参数
当前分块参数是硬编码的，可以：
- 支持用户自定义chunk_size和chunk_overlap
- 根据文档类型调整参数

#### 3. 更智能的分割点
当前只查找句号和换行符，可以扩展：
- 支持更多标点符号（！？；等）
- 识别段落标记
- 考虑中英文混合情况

---

## 5. 技术细节总结

| 项目 | 当前实现 |
|------|---------|
| **向量维度** | 384维 |
| **嵌入模型** | paraphrase-multilingual-MiniLM-L12-v2 |
| **分块方式** | 启发式分块（字符数+智能分割点） |
| **块大小** | 500字符（默认） |
| **重叠大小** | 50字符（默认） |
| **分割点查找** | 句号（。）、换行符（\n） |
| **是否语义分块** | ❌ 否（基于字符数+简单启发式） |
| **是否均等切割** | ⚠️ 部分（尽量在分割点处，否则均等） |

---

## 6. 代码位置

- **向量生成：** `server/vector_service.py` 第64-87行
- **文本分块：** `server/vector_service.py` 第89-125行
- **文档添加：** `server/vector_service.py` 第127-185行











