# 向量化服务超时优化说明

## 问题描述

MCP服务器在初始化向量化服务时，如果网络无法访问HuggingFace，会长时间阻塞在模型下载阶段，导致：
- 服务器无法完成初始化
- 端口8000无法监听
- 其他功能（LLM代理、会话管理等）无法使用

## 解决方案

### 实施内容

在 `server/mcp_server.py` 中添加了超时机制：

1. **使用线程隔离初始化**
   - 在单独线程中初始化向量化服务
   - 避免阻塞主线程

2. **设置超时限制**
   - 最多等待30秒
   - 如果超时，跳过向量化服务初始化
   - 服务器继续启动，其他功能正常

3. **优雅降级**
   - 向量化服务初始化失败时，设置为 `None`
   - 相关工具调用会返回友好的错误提示
   - 不影响其他核心功能

### 代码实现

```python
# 初始化向量化服务（使用超时机制，避免长时间阻塞）
self.vector_service = None
try:
    import threading
    from vector_service import VectorService
    
    # 使用线程和超时机制初始化向量化服务
    vector_service_instance = [None]
    init_error = [None]
    
    def init_vector_service():
        try:
            vector_service_instance[0] = VectorService()
        except Exception as e:
            init_error[0] = e
    
    # 在单独线程中初始化，设置超时
    init_thread = threading.Thread(target=init_vector_service, daemon=True)
    init_thread.start()
    init_thread.join(timeout=30)  # 最多等待30秒
    
    if init_thread.is_alive():
        print("[MCP Server] ⚠️  警告：向量化服务初始化超时（30秒），跳过初始化")
        print("[MCP Server] 提示：向量化服务将在后台继续初始化，但服务器已继续启动")
        self.vector_service = None
    elif init_error[0]:
        print(f"[MCP Server] ⚠️  警告：向量化服务初始化失败: {init_error[0]}")
        self.vector_service = None
    elif vector_service_instance[0]:
        print("[MCP Server] ✅ 向量化服务初始化成功")
        self.vector_service = vector_service_instance[0]
    else:
        self.vector_service = None
except Exception as e:
    print(f"[MCP Server] ⚠️  警告：向量化服务初始化异常: {e}")
    self.vector_service = None
```

## 效果

### 优化前
- ❌ 服务器卡在模型加载阶段
- ❌ 端口8000无法监听
- ❌ 所有功能无法使用

### 优化后
- ✅ 服务器在30秒内完成初始化
- ✅ 端口8000正常监听
- ✅ 核心功能（LLM代理、会话管理、文件服务）正常
- ⚠️ 向量化功能可能不可用（如果初始化失败）

## 功能影响

### 正常工作的功能
1. **LLM代理** - 正常
2. **会话管理** - 正常
3. **文件服务** - 正常
4. **资源接口** - 正常（法律文书模板、法律法规等）
5. **提示词模板** - 正常

### 可能受影响的功能
1. **向量化工具** - 如果初始化失败，相关工具调用会返回错误提示
2. **相似文档搜索** - 如果向量化服务不可用，此功能不可用

## 使用建议

1. **如果向量化服务初始化失败**：
   - 服务器仍可正常使用其他功能
   - 可以稍后重启服务器，尝试重新初始化
   - 或修复网络问题后重启

2. **如果需要使用向量化功能**：
   - 确保网络可以访问HuggingFace
   - 或使用本地模型缓存
   - 参考 `模型下载问题解决方案.md`

3. **检查向量化服务状态**：
   - 查看启动日志中的 `[VectorService]` 和 `[MCP Server]` 消息
   - 如果看到 "✅ 向量化服务初始化成功"，说明服务正常
   - 如果看到 "⚠️ 警告" 或 "超时"，说明服务不可用

## 技术细节

- **超时时间**：30秒（可根据需要调整）
- **线程类型**：daemon线程（主进程退出时自动终止）
- **错误处理**：多层异常捕获，确保不会导致服务器崩溃
- **日志输出**：详细的日志信息，便于问题诊断

## 总结

通过添加超时机制，MCP服务器现在可以在向量化服务初始化失败时继续启动，确保核心功能可用。这是一个优雅的降级方案，提高了系统的可用性和稳定性。






