# 工具资源调用问题诊断报告

## 问题描述

模型识别到需要调用工具，但客户端没有调用相关工具资源接口。

## 可能的原因分析

### 1. 响应格式不匹配

**问题**: 模型返回的响应格式可能不符合客户端预期的格式。

**客户端支持的格式**:

#### 格式1: 直接格式
```json
{
  "type": "invoke_tool_or_resource",
  "resource_uri": "legal://similar_cases",
  "parameters": {
    "case_description": "合同纠纷"
  }
}
```

#### 格式2: type: 7 嵌套格式
```json
{
  "type": 7,
  "data": {
    "lawQaText": "{\"Type\":\"invoke_tool_or_resource\",\"resource_uri\":\"legal://similar_cases\",\"parameters\":{\"case_description\":\"合同纠纷\"}}"
  }
}
```

**检查点**:
- 模型返回的 `type` 字段值是什么？
- 如果是 `type: 7`，`lawQaText` 字段是否存在？
- `lawQaText` 中的 `Type` 字段值是否正确（大小写敏感）？

### 2. JSON解析失败

**代码位置**: `mcp_client.js` 第1620-1645行

**问题**: 如果JSON解析失败，会直接显示响应，不会检测工具调用需求。

**检查点**:
- 控制台是否有 "JSON解析失败" 的错误日志？
- 响应中的JSON格式是否完整（可能有截断）？

### 3. 流式处理中未正确检测

**代码位置**: `mcp_client.js` 第1359-1382行（流式回调）

**问题**: 在流式过程中，如果 `lawText` 是JSON字符串但不完整，可能无法正确检测。

**检查点**:
- 流式过程中是否有 "🔧 流式过程中检测到invoke_tool_or_resource类型" 的日志？
- `lawText` 是否在流式过程中被正确接收？

### 4. 响应类型检查逻辑问题

**代码位置**: `mcp_client.js` 第1673行

**当前检查逻辑**:
```javascript
if (responseType === 'invoke_tool_or_resource' || responseType === 'invoke_tool' || responseType === 'invoke_prompt') {
  // 调用工具/资源
}
```

**问题**: 
- 如果模型返回的是其他类型（如 `type: 4`），但实际需要调用工具，可能无法检测到。

### 5. lawQaText 解析问题

**代码位置**: `mcp_client.js` 第2771行（`parseInvokeToolOrResource` 函数）

**检查逻辑**:
```javascript
if (parsed.Type === 'invoke_tool_or_resource' || parsed.type === 'invoke_tool_or_resource') {
  // 解析参数
}
```

**问题**:
- `Type` 字段的大小写是否匹配？
- `lawQaText` 是否是有效的JSON字符串？

## 诊断步骤

### 步骤1: 检查浏览器控制台日志

查看是否有以下日志：
- `🔧 检测到invoke_tool_or_resource类型，准备调用工具/资源`
- `🔧 流式过程中检测到invoke_tool_or_resource类型`
- `JSON解析成功，type: ...`
- `JSON解析失败`

### 步骤2: 检查模型返回的响应格式

在浏览器控制台中，查看：
- `响应前200字符:` 日志
- `JSON解析成功，type:` 日志
- `完整响应:` 日志

### 步骤3: 检查响应类型

确认模型返回的响应中：
- `type` 字段的值是什么？
- 如果是 `type: 7`，`data.lawQaText` 的内容是什么？
- `lawQaText` 中的 `Type` 字段值是什么？

### 步骤4: 检查参数提取

确认 `parseInvokeToolOrResource` 函数是否正确提取了参数：
- `resource_uri` 是否存在？
- `tool_name` 是否存在？
- `parameters` 是否包含必要参数？

## 可能的问题场景

### 场景1: 模型返回的格式不符合预期

**现象**: 模型返回了需要调用工具的意图，但格式不是 `invoke_tool_or_resource`。

**解决方案**: 
- 检查系统提示词，确保模型理解需要返回的格式
- 检查模型返回的实际格式，调整解析逻辑

### 场景2: JSON解析失败

**现象**: 响应中包含JSON，但解析失败。

**可能原因**:
- JSON格式不完整（流式截断）
- JSON中包含特殊字符
- JSON格式错误

**解决方案**:
- 增强JSON解析的错误处理
- 检查流式处理逻辑，确保JSON完整

### 场景3: 流式处理中检测失败

**现象**: 在流式过程中，`lawText` 是JSON字符串但不完整，无法检测。

**解决方案**:
- 在流式过程中，如果检测到JSON不完整，等待完整JSON后再处理
- 在最终解析时再次检查

### 场景4: 响应类型检查逻辑不完整

**现象**: 模型返回了其他类型，但实际需要调用工具。

**解决方案**:
- 扩展响应类型检查逻辑
- 在最终解析时，检查所有可能的格式

## 调试建议

### 1. 添加详细日志

在关键位置添加日志：
```javascript
console.log('=== 响应解析调试 ===');
console.log('响应类型:', responseData.type);
console.log('响应数据:', JSON.stringify(responseData, null, 2));
if (responseData.type === 7 && responseData.data && responseData.data.lawQaText) {
  console.log('lawQaText内容:', responseData.data.lawQaText);
  const invokeInfo = parseInvokeToolOrResource(responseData.data.lawQaText);
  console.log('解析结果:', invokeInfo);
}
```

### 2. 检查模型返回的实际格式

在浏览器控制台中，查看完整的响应内容，确认：
- 响应格式是否符合预期
- JSON是否完整
- 字段名称是否正确

### 3. 测试不同的响应格式

测试以下场景：
- 直接格式：`type: 'invoke_tool_or_resource'`
- 嵌套格式：`type: 7` 且 `lawQaText` 包含 `Type: 'invoke_tool_or_resource'`
- 其他可能的格式

## 结论

**不是SDK集成问题**。客户端已经实现了MCP协议的调用逻辑（`sendMCPRequest` 函数）。

**可能的原因**:
1. 模型返回的响应格式不符合客户端预期
2. JSON解析失败
3. 响应类型检查逻辑不完整
4. 流式处理中检测失败

**建议**:
1. 检查浏览器控制台日志，确认响应格式
2. 检查模型返回的实际内容
3. 根据实际情况调整解析逻辑






