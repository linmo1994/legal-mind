# 资源调用重复显示问题诊断

## 问题描述

客户端只调用了一次资源，但前端重复循环展示资源调用的内容。

## 代码流程分析

### 资源调用流程

**位置**: `mcp_client.js` 第2062行 (`handleResourceInvocation`)

**流程**:
1. 第2078行：调用 `addToolInvocationMessage` 显示资源调用参数
2. 第2106-2112行：调用MCP资源接口
3. 第2123行：调用 `updateToolInvocationMessage` 更新资源调用结果
4. 第2138-2140行：构建请求数据，包含资源数据
5. 第2143行：调用 `addStreamingMessage` 创建流式消息
6. 第2182行：调用 `callLLM` 继续生成最终结果
7. 第2185行：调用 `parseAndHandleResponse` 解析LLM响应

### 问题分析

**可能的原因**:

#### 原因1: parseAndHandleResponse 再次触发资源调用

**位置**: `mcp_client.js` 第1700-1781行

**问题**:
- `parseAndHandleResponse` 在解析LLM响应时，可能会再次检测到 `invoke_tool_or_resource` 类型
- 如果LLM的响应中又包含了资源调用请求，会再次调用 `handleResourceInvocation`
- 导致资源调用被重复执行和显示

**检查点**:
```javascript
// 第1702行：检查type: 7格式
if (responseData.type === 7 && responseData.data && responseData.data.lawQaText) {
  const invokeInfo = parseInvokeToolOrResource(responseData.data.lawQaText);
  if (invokeInfo) {
    // 再次调用资源/工具
    await handleResponseByType(invokeResponseData, thinkingContent, '');
  }
}
```

#### 原因2: 资源调用结果被重复添加到DOM

**位置**: `mcp_client.js` 第2123行、第2185行

**问题**:
- `updateToolInvocationMessage` 更新资源调用结果
- `parseAndHandleResponse` 可能又将资源调用结果添加到DOM
- 导致资源调用内容被重复显示

#### 原因3: 流式处理中重复显示

**位置**: `mcp_client.js` 第2148-2180行

**问题**:
- 流式回调函数 `onStreamChunk` 可能会重复更新消息
- 如果流式响应中包含资源调用信息，可能会重复显示

## 解决方案

### 方案1: 在资源调用后，防止再次触发资源调用

**修改位置**: `mcp_client.js` 第2185行

**修改内容**:
- 在 `handleResourceInvocation` 中调用 `parseAndHandleResponse` 时，传入一个标志，表示已经处理过资源调用
- 在 `parseAndHandleResponse` 中检查该标志，如果已经处理过资源调用，不再重复处理

### 方案2: 检查响应是否包含资源调用请求

**修改位置**: `mcp_client.js` 第1700-1781行

**修改内容**:
- 在 `parseAndHandleResponse` 中，如果检测到 `invoke_tool_or_resource` 类型，检查是否已经在资源调用流程中
- 如果已经在资源调用流程中（通过检查上下文或标志），不再重复调用

### 方案3: 清理已显示的资源调用信息

**修改位置**: `mcp_client.js` 第2123行、第2185行

**修改内容**:
- 在 `parseAndHandleResponse` 中，如果检测到是资源调用后的响应，清理之前显示的资源调用信息
- 或者，在显示最终结果时，不重复显示资源调用信息

## 推荐解决方案

**方案2** 是最合适的，因为：
1. 资源调用后，LLM的响应不应该再包含资源调用请求
2. 如果LLM的响应中包含了资源调用请求，说明逻辑有问题
3. 应该在资源调用流程中，标记已经处理过资源调用，避免重复处理

### 具体实现

在 `handleResourceInvocation` 函数中：
1. 添加一个标志，表示正在处理资源调用
2. 在调用 `parseAndHandleResponse` 时，传入该标志
3. 在 `parseAndHandleResponse` 中，如果检测到 `invoke_tool_or_resource` 类型，检查是否已经在资源调用流程中
4. 如果已经在资源调用流程中，直接显示响应内容，不再重复调用资源






