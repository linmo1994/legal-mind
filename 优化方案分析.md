# 优化方案分析：直接使用 requestData

## 当前实现的问题

### 1. 数据重复
- `requestData` 中已经包含 `system_prompt`
- 客户端又单独提取 `CONFIG.systemPrompt` 作为 system 消息
- 导致 system_prompt 被传递了两次

### 2. 数据格式浪费
- 将结构化的 `requestData` 序列化为 JSON 字符串
- 放在 user 消息的 content 中：`请根据以下数据分析和回答：\n\n${requestText}`
- 模型需要解析 JSON 字符串才能获取结构化数据

### 3. 历史对话未利用
- `requestData.conversation_history` 包含历史对话（数组格式）
- 但没有被转换为 messages 格式
- 而是作为字符串的一部分，模型无法正确理解上下文

### 4. 职责不清
- 客户端需要知道 DeepSeek API 的 messages 格式要求
- 应该由服务端负责格式转换

## 优化方案

### 客户端修改
直接发送 `requestData`，不构建 messages 数组：

```javascript
// 修改前
const messages = [
  { role: 'system', content: CONFIG.systemPrompt },
  { role: 'user', content: `请根据以下数据分析和回答：\n\n${requestText}` }
];
const requestBody = {
  model: CONFIG.llmModel,
  messages: messages,
  temperature: 0.0,
  max_tokens: 2048,
  stream: true
};

// 修改后
const requestBody = {
  ...requestData,  // 直接使用 requestData
  stream: true
};
```

### 服务端修改
根据 `requestData` 构建 messages 数组：

```python
# 从 requestData 中提取信息
system_prompt = request_data.get('system_prompt', '')
conversation_history = request_data.get('conversation_history', [])
current_user_input = request_data.get('current_user_input', '')

# 构建 messages 数组
messages = []
if system_prompt:
    messages.append({"role": "system", "content": system_prompt})

# 将历史对话转换为 messages 格式
for msg in conversation_history:
    messages.append({
        "role": msg.get('role', 'user'),
        "content": msg.get('content', '')
    })

# 添加当前用户输入
if current_user_input:
    messages.append({"role": "user", "content": current_user_input})

# 构建 LLM 请求
llm_request = {
    "model": request_data.get('model', model),
    "messages": messages,
    "temperature": request_data.get('config', {}).get('temperature', 0.0),
    "max_tokens": request_data.get('config', {}).get('max_tokens', 2048),
    "stream": request_data.get('stream', False)
}
```

## 优势

1. **职责分离**：客户端只负责构建业务数据，服务端负责 API 格式转换
2. **利用历史对话**：正确地将历史对话转换为 messages 格式
3. **数据不重复**：system_prompt 只传递一次
4. **更灵活**：服务端可以根据需要调整 messages 的构建逻辑
5. **更清晰**：代码逻辑更直观，易于维护

