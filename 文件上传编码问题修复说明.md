# 文件上传编码问题修复说明

## 问题描述
上传文件时出现错误：
```
'utf-8' codec can't decode byte 0xdc in position 232: invalid continuation byte
```

## 问题原因
1. **文件名编码问题**：当文件名包含非UTF-8字符（如GBK、GB2312编码的中文文件名）时，在解析multipart/form-data的header时，直接使用UTF-8解码会导致失败。

2. **JSON序列化问题**：在返回文件信息时，如果文件名包含无法用UTF-8编码的字符，`json.dumps()`可能会失败。

## 修复方案

### 1. 文件名解析增强（`server/mcp_server.py`）

**修复前**：
```python
header_str = header.decode('utf-8', errors='ignore')
# 直接从header_str提取文件名
original_filename = header_str[filename_start:filename_end]
```

**修复后**：
- 使用`errors='replace'`替代`errors='ignore'`，确保解码不会失败
- 如果UTF-8解码失败，回退到latin-1编码（不会失败）
- 从bytes中直接提取文件名，尝试多种编码（UTF-8、latin-1、GBK、GB2312）
- 如果所有编码都失败，使用latin-1并替换无法解码的字符

```python
# 使用更安全的解码方式处理header
try:
    header_str = header.decode('utf-8', errors='replace')
except Exception:
    header_str = header.decode('latin-1', errors='replace')

# 从bytes中直接提取文件名，尝试多种编码
for encoding in ['utf-8', 'latin-1', 'gbk', 'gb2312']:
    try:
        filename_match = filename_bytes_part.decode(encoding)
        break
    except (UnicodeDecodeError, UnicodeError):
        continue
```

### 2. 文件名验证和清理（`server/mcp_server.py`）

在保存文件前，确保文件名是有效的Unicode字符串：
```python
# 确保文件名是有效的Unicode字符串
try:
    if isinstance(original_filename, bytes):
        original_filename = original_filename.decode('utf-8', errors='replace')
    elif not isinstance(original_filename, str):
        original_filename = str(original_filename)
except Exception as e:
    print(f"[File Upload] 文件名处理警告: {e}")
    original_filename = "uploaded_file"
```

### 3. JSON响应安全序列化（`server/mcp_server.py`）

在返回JSON响应前，清理所有字符串字段，确保可以安全序列化：
```python
# 清理file_info中的字符串，确保可以安全序列化为JSON
safe_file_info = {}
for key, value in file_info.items():
    if isinstance(value, str):
        try:
            value.encode('utf-8')
            safe_file_info[key] = value
        except UnicodeEncodeError:
            # 如果编码失败，使用replace策略
            safe_file_info[key] = value.encode('utf-8', errors='replace').decode('utf-8', errors='replace')
    else:
        safe_file_info[key] = value

# 尝试使用ensure_ascii=False，如果失败则回退到ensure_ascii=True
try:
    response_json = json.dumps(safe_file_info, ensure_ascii=False)
    self.wfile.write(response_json.encode('utf-8'))
except Exception as e:
    print(f"[File Upload] JSON序列化错误: {e}")
    response_json = json.dumps(safe_file_info, ensure_ascii=True)
    self.wfile.write(response_json.encode('utf-8'))
```

### 4. 日志输出安全处理（`server/file_service.py`）

在打印文件名时，使用安全的编码方式：
```python
# 安全打印文件名（处理编码问题）
try:
    safe_filename = original_filename.encode('utf-8', errors='replace').decode('utf-8', errors='replace')
    print(f"[FileService] 文件已保存: {safe_filename} -> {file_path}")
except Exception:
    print(f"[FileService] 文件已保存: [文件名包含特殊字符] -> {file_path}")
```

## 修复效果

1. ✅ **支持各种编码的文件名**：UTF-8、GBK、GB2312等编码的文件名都可以正常上传
2. ✅ **安全的错误处理**：即使遇到无法解码的字符，也会使用替换策略，不会导致服务器崩溃
3. ✅ **JSON响应稳定**：确保返回的JSON响应始终可以正确序列化
4. ✅ **日志输出安全**：避免因文件名编码问题导致日志输出失败

## 测试建议

1. **测试UTF-8文件名**：
   - 上传文件名：`测试文件.txt`
   - 预期：正常上传

2. **测试GBK编码文件名**：
   - 上传文件名：`测试文档.doc`（GBK编码）
   - 预期：正常上传，文件名正确显示

3. **测试特殊字符文件名**：
   - 上传文件名：`file with spaces.txt`
   - 预期：正常上传

4. **测试二进制文件**：
   - 上传图片、PDF等二进制文件
   - 预期：正常上传，文件内容完整

## 注意事项

1. 如果文件名包含无法用任何编码正确解码的字符，会使用`errors='replace'`策略，可能会显示为``字符
2. 文件内容本身不受影响，只有文件名的显示可能会有轻微变化
3. 建议用户尽量使用UTF-8编码的文件名，以获得最佳兼容性






