# 资源调用无响应问题诊断

## 问题描述

客户端发起资源调用请求后，服务端未能响应。

## 代码流程分析

### 客户端请求流程

**位置**: `mcp_client.js` 第2076行

```javascript
const resourceResponse = await sendMCPRequest({
  method: 'resources/read',
  params: {
    uri: resourceUri,
    arguments: parameters
  }
});
```

**请求格式**:
```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "resources/read",
  "params": {
    "uri": "legal://similar_cases",
    "arguments": {
      "case_description": "合同纠纷"
    }
  }
}
```

**请求URL**: `http://localhost:8000` (CONFIG.mcpServerUrl)

### 服务端处理流程

**位置**: `server/mcp_server.py`

#### 步骤1: HTTP请求路由 (第1222行)

```python
def do_POST(self):
    path = self.path.split('?')[0]
    
    if path == '/api/llm/chat':
        self._handle_llm_proxy()
        return
    
    if path.startswith('/api/sessions'):
        self._handle_session_api(path)
        return
    
    if path == '/api/files/upload':
        self._handle_file_upload()
        return
    
    # 识别为MCP协议请求
    request = json.loads(post_data.decode('utf-8'))
    response = self.server_instance.handle_request(request)
```

#### 步骤2: MCP请求处理 (第446行)

```python
def handle_request(self, request: Dict) -> Optional[Dict]:
    method = request.get("method")
    params = request.get("params", {})
    request_id = request.get("id")
    
    if method == "resources/read":
        return self._handle_resource_read(request_id, params)
```

#### 步骤3: 资源读取处理 (第728行)

```python
def _handle_resource_read(self, request_id: Any, params: Dict) -> Dict:
    uri = params.get("uri")
    arguments = params.get("arguments", {})
    
    # 处理资源读取...
    return {
        "jsonrpc": "2.0",
        "id": request_id,
        "result": {
            "contents": [...]
        }
    }
```

## 可能的问题

### 问题1: 服务端未启动或端口不正确

**检查方法**:
1. 检查服务端是否正在运行
2. 检查端口8000是否被占用
3. 检查服务端日志是否有错误

**验证命令**:
```bash
# 检查端口8000是否在监听
lsof -i :8000

# 检查服务端进程
ps aux | grep mcp_server
```

### 问题2: 请求格式不正确

**检查点**:
- 客户端发送的请求体格式是否正确
- `method` 字段是否为 `"resources/read"`
- `params` 中是否包含 `uri` 和 `arguments`

**调试方法**:
- 在浏览器控制台查看 `sendMCPRequest` 的请求日志
- 检查服务端日志中的 `[DEBUG] 收到POST请求` 和 `[DEBUG] 资源读取请求`

### 问题3: 服务端异常未捕获

**检查点**:
- 服务端 `_handle_resource_read` 函数是否抛出异常
- 异常是否被正确捕获并返回错误响应

**调试方法**:
- 查看服务端日志中的异常信息
- 检查是否有 `Internal error` 或 `Exception` 日志

### 问题4: 响应未正确发送

**检查点**:
- 服务端是否调用了 `self.wfile.write()`
- 响应头是否已发送
- 响应体是否为空

**调试方法**:
- 检查服务端日志中的响应发送日志
- 在浏览器网络面板中查看响应状态码和响应体

### 问题5: 客户端请求超时

**检查点**:
- `sendMCPRequest` 的超时设置
- 服务端处理时间是否过长

**调试方法**:
- 检查浏览器控制台是否有超时错误
- 检查服务端处理时间

## 调试步骤

### 步骤1: 检查服务端日志

查看服务端控制台输出，确认：
1. 是否收到POST请求：`[DEBUG] 收到POST请求，路径: /`
2. 是否识别为MCP协议请求：`[DEBUG] 识别为MCP协议请求`
3. 是否处理资源读取请求：`[DEBUG] 资源读取请求 - URI: ..., 参数: ...`
4. 是否有异常信息

### 步骤2: 检查浏览器网络面板

在浏览器开发者工具的 Network 标签中：
1. 查找对 `http://localhost:8000` 的POST请求
2. 检查请求状态码（200/500/超时）
3. 查看请求体内容
4. 查看响应体内容（如果有）

### 步骤3: 检查浏览器控制台

查看客户端日志：
1. `调用MCP资源: ...` 日志
2. `资源调用成功` 或错误日志
3. 网络错误或超时错误

### 步骤4: 检查请求格式

在浏览器控制台中，查看 `sendMCPRequest` 发送的实际请求：
```javascript
console.log('MCP请求:', {
  method: 'resources/read',
  params: {
    uri: resourceUri,
    arguments: parameters
  }
});
```

## 常见问题解决方案

### 问题1: 服务端未启动

**解决方案**:
```bash
cd server
python3 mcp_server.py
```

### 问题2: 端口被占用

**解决方案**:
```bash
# 查找占用端口的进程
lsof -i :8000

# 杀死进程
kill -9 <PID>
```

### 问题3: 请求格式错误

**解决方案**:
- 检查客户端发送的请求格式
- 确保 `method` 为 `"resources/read"`
- 确保 `params` 包含 `uri` 和 `arguments`

### 问题4: 服务端异常

**解决方案**:
- 查看服务端日志中的异常堆栈
- 检查 `_handle_resource_read` 函数的实现
- 确保所有异常都被正确捕获

## 验证代码

### 客户端请求验证

在 `sendMCPRequest` 函数中添加详细日志：
```javascript
console.log('=== MCP请求详情 ===');
console.log('URL:', CONFIG.mcpServerUrl);
console.log('请求体:', JSON.stringify(mcpRequest, null, 2));
```

### 服务端响应验证

在 `do_POST` 方法中添加详细日志：
```python
print(f"[DEBUG] 请求体: {post_data.decode('utf-8')}")
print(f"[DEBUG] 解析后的请求: {request}")
print(f"[DEBUG] 响应: {response}")
```

## 总结

**可能的原因**:
1. 服务端未启动或端口不正确
2. 请求格式不正确
3. 服务端异常未捕获
4. 响应未正确发送
5. 客户端请求超时

**建议**:
1. 检查服务端是否正常运行
2. 查看服务端和客户端日志
3. 检查浏览器网络面板中的请求和响应
4. 验证请求格式是否正确






