# 客户端发送数据流分析报告

## 数据流概览

当客户端点击"发送"按钮后，数据流如下：

```
用户输入 → fullUserMessage → buildRequestData() → callLLM() → 服务端 → LLM API
```

## 详细分析

### 1. 客户端数据构建 (`buildRequestData`)

**位置**: `mcp_client.js:1078-1106`

**函数签名**:
```javascript
function buildRequestData(userInput) {
  return {
    system_prompt: CONFIG.systemPrompt,        // ✅ 系统提示词
    tools: [],
    resources: [...],
    session: {...},
    conversation_history: currentSession.conversationHistory,  // ✅ 历史对话
    current_user_input: userInput,             // ✅ 当前用户输入
    config: {
      temperature: 0.0,
      max_tokens: 2048,
      response_format: 'text'
    }
  };
}
```

**关键字段**:
- ✅ `system_prompt`: 从 `CONFIG.systemPrompt` 获取（从 `config.json` 加载）
- ✅ `conversation_history`: 从 `currentSession.conversationHistory` 获取
- ✅ `current_user_input`: 传入的 `userInput` 参数（即 `fullUserMessage`）

### 2. 系统提示词加载

**位置**: `mcp_client.js:186`

**加载流程**:
1. 从 `config.json` 加载配置
2. 将 `configData.system_prompt` 赋值给 `CONFIG.systemPrompt`
3. 在 `buildRequestData` 中使用 `CONFIG.systemPrompt`

**验证点**:
```javascript
CONFIG.systemPrompt = configData.system_prompt;
console.log('System Prompt长度:', CONFIG.systemPrompt?.length || 0);
```

### 3. 请求发送 (`callLLM`)

**位置**: `mcp_client.js:776-1075`

**发送流程**:
```javascript
const requestBody = {
  ...requestData,  // ✅ 包含 system_prompt, conversation_history, current_user_input
  stream: stream
};

// 发送到服务端
const response = await fetch(proxyUrl, {
  method: 'POST',
  headers: headers,
  body: JSON.stringify(requestBody),  // ✅ 完整发送所有数据
  signal: controller.signal
});
```

**日志输出**:
```javascript
console.log('请求体结构:', {
  hasSystemPrompt: !!requestBody.system_prompt,              // ✅ 检查系统提示词
  conversationHistoryCount: requestBody.conversation_history?.length || 0,  // ✅ 检查历史对话
  currentUserInput: requestBody.current_user_input?.substring(0, 50) || '',  // ✅ 检查当前输入
  resourcesCount: requestBody.resources?.length || 0,
  stream: stream
});
```

### 4. 服务端接收和处理

**位置**: `server/mcp_server.py:1324-1383`

**处理流程**:
1. **识别请求格式**:
   - 检查是否有 `messages` 数组（旧格式）
   - 或检查是否有 `current_user_input`（新格式）

2. **提取数据**:
   ```python
   system_prompt = request_data.get('system_prompt', '')           # ✅ 提取系统提示词
   conversation_history = request_data.get('conversation_history', [])  # ✅ 提取历史对话
   current_user_input = request_data.get('current_user_input', '')  # ✅ 提取当前用户输入
   ```

3. **构建 messages 数组**:
   ```python
   messages = []
   
   # 1. 添加 system 消息
   if system_prompt:
       messages.append({"role": "system", "content": system_prompt})
   
   # 2. 添加历史对话
   for msg in conversation_history:
       if isinstance(msg, dict) and msg.get('content'):
           messages.append({"role": msg.get('role'), "content": msg.get('content')})
   
   # 3. 添加当前用户输入
   if current_user_input:
       messages.append({"role": "user", "content": current_user_input})
   ```

4. **发送给 LLM API**:
   ```python
   llm_request = {
       "model": model,
       "messages": messages,  # ✅ 包含 system、历史对话和当前输入
       "temperature": temperature,
       "max_tokens": max_tokens,
       "stream": stream
   }
   ```

## 验证检查点

### ✅ 客户端检查点

1. **系统提示词加载**:
   - [x] `CONFIG.systemPrompt` 从 `config.json` 加载
   - [x] `buildRequestData` 中包含 `system_prompt: CONFIG.systemPrompt`
   - [x] 日志输出 `hasSystemPrompt: true`

2. **当前用户输入**:
   - [x] `fullUserMessage` 正确构建（包含文本和文件）
   - [x] `buildRequestData(fullUserMessage)` 正确调用
   - [x] `requestData.current_user_input` 包含用户输入
   - [x] 日志输出 `currentUserInput` 不为空

3. **历史对话**:
   - [x] `currentSession.conversationHistory` 正确获取
   - [x] `requestData.conversation_history` 包含历史对话
   - [x] 日志输出 `conversationHistoryCount` 正确

4. **请求发送**:
   - [x] `callLLM` 正确发送 `requestData`
   - [x] 请求体包含所有必要字段
   - [x] 日志输出完整的请求体 JSON

### ✅ 服务端检查点

1. **数据接收**:
   - [x] 服务端正确识别新格式（包含 `current_user_input`）
   - [x] `system_prompt` 正确提取
   - [x] `conversation_history` 正确提取
   - [x] `current_user_input` 正确提取

2. **messages 构建**:
   - [x] system 消息正确添加
   - [x] 历史对话正确转换
   - [x] 当前用户输入正确添加
   - [x] messages 数组不为空

3. **日志输出**:
   - [x] 服务端日志显示 `system_prompt存在: True`
   - [x] 服务端日志显示 `current_user_input存在: True`
   - [x] 服务端日志显示 `最终构建的messages数组长度 > 0`

## 潜在问题

### 问题1: 系统提示词为空

**可能原因**:
- `config.json` 中 `system_prompt` 字段缺失或为空
- 配置文件加载失败

**检查方法**:
```javascript
console.log('System Prompt长度:', CONFIG.systemPrompt?.length || 0);
```

### 问题2: 当前用户输入为空

**可能原因**:
- `fullUserMessage` 构建失败
- `userInput` 为空且没有文件

**检查方法**:
```javascript
console.log('准备调用 buildRequestData，fullUserMessage 类型:', typeof fullUserMessage, '长度:', fullUserMessage?.length);
```

### 问题3: 服务端识别为旧格式

**可能原因**:
- 客户端发送的数据格式不正确
- 服务端检测逻辑有问题

**检查方法**:
查看服务端日志：
```
[DEBUG] 检测到新格式（requestData），构建messages数组
```

## 调试建议

1. **检查浏览器控制台**:
   - 查看 `请求数据已组装` 日志
   - 查看 `客户端发送给代理服务的请求数据` 日志
   - 确认 `hasSystemPrompt: true`
   - 确认 `currentUserInput` 不为空

2. **检查服务端日志**:
   - 查看 `[DEBUG] system_prompt存在: True`
   - 查看 `[DEBUG] current_user_input存在: True`
   - 查看 `[DEBUG] 最终构建的messages数组长度`

3. **检查配置文件**:
   - 确认 `config.json` 中存在 `system_prompt` 字段
   - 确认 `system_prompt` 内容不为空

## 结论

✅ **客户端代码正确携带了系统提示词和当前用户指令**:
- `system_prompt` 从 `CONFIG.systemPrompt` 获取并包含在请求中
- `current_user_input` 从 `fullUserMessage` 获取并包含在请求中
- `conversation_history` 从 `currentSession.conversationHistory` 获取并包含在请求中

✅ **服务端代码正确处理了这些数据**:
- 正确识别新格式
- 正确提取 `system_prompt`、`conversation_history` 和 `current_user_input`
- 正确构建 messages 数组并发送给 LLM API

如果遇到问题，请检查：
1. 浏览器控制台的日志输出
2. 服务端日志的详细输出
3. `config.json` 配置文件的内容






