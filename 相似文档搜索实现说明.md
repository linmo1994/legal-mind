# 相似文档搜索返回数量实现说明

## 实现流程

### 1. 客户端请求

**位置：** `vectorize.html` - `searchDocuments()` 函数

```javascript
const nResults = parseInt(document.getElementById('searchResults').value) || 5;

const results = await callTool('search_documents', {
  query: query,
  n_results: nResults  // 用户设置的结果数量（默认5）
});
```

**说明：**
- 用户在前端输入框中设置返回结果数量（1-20）
- 默认值为 5
- 通过 MCP 协议传递给服务端

---

### 2. MCP服务端接收参数

**位置：** `server/mcp_server.py` - `_handle_tool_call()` 方法

```python
elif tool_name == "search_documents":
    query = arguments.get("query")
    n_results = arguments.get("n_results", 5)  # 从客户端获取，默认5
    
    if not query:
        return self._error_response(...)
    
    results = self.vector_service.search(query, n_results)  # 传递给向量服务
```

**说明：**
- 从 `arguments` 字典中获取 `n_results` 参数
- 如果未提供，使用默认值 5
- 将参数传递给 `VectorService.search()` 方法

---

### 3. 向量服务执行搜索

**位置：** `server/vector_service.py` - `search()` 方法

```python
def search(self, query: str, n_results: int = 5) -> List[Dict]:
    """
    搜索相似文档
    
    Args:
        query: 查询文本
        n_results: 返回结果数量（默认5）
        
    Returns:
        相似文档列表
    """
    try:
        # 1. 生成查询向量
        query_embedding = self._generate_embedding(query)
        
        # 2. 调用ChromaDB的query方法，传入n_results参数
        results = self.collection.query(
            query_embeddings=[query_embedding],
            n_results=n_results  # 控制返回结果数量
        )
        
        # 3. 格式化结果
        formatted_results = []
        if results['ids'] and len(results['ids'][0]) > 0:
            for i in range(len(results['ids'][0])):  # 遍历返回的结果
                formatted_results.append({
                    "id": results['ids'][0][i],
                    "document": results['documents'][0][i],
                    "metadata": results['metadatas'][0][i],
                    "distance": results['distances'][0][i] if 'distances' in results else None
                })
        
        return formatted_results
```

**关键点：**
- `n_results` 参数直接传递给 ChromaDB 的 `query()` 方法
- ChromaDB 会根据向量相似度返回最相似的 `n_results` 个结果
- 结果按相似度从高到低排序（距离越小，相似度越高）

---

### 4. ChromaDB底层实现

**ChromaDB `query()` 方法的工作原理：**

```python
collection.query(
    query_embeddings=[query_embedding],  # 查询向量
    n_results=n_results                  # 返回最相似的n_results个结果
)
```

**内部实现逻辑：**
1. **向量相似度计算**：使用余弦相似度或欧氏距离计算查询向量与所有文档向量的相似度
2. **排序**：按相似度从高到低排序
3. **截取**：返回前 `n_results` 个最相似的结果
4. **返回数据**：返回每个结果的 ID、文档内容、元数据和距离值

---

## 参数传递链路

```
客户端 (vectorize.html)
  ↓ n_results (用户输入，默认5)
MCP服务端 (mcp_server.py)
  ↓ n_results (传递给向量服务)
向量服务 (vector_service.py)
  ↓ n_results (传递给ChromaDB)
ChromaDB (底层向量数据库)
  ↓ 返回n_results个最相似的结果
向量服务 (格式化结果)
  ↓ 返回格式化后的列表
MCP服务端 (JSON序列化)
  ↓ 返回JSON响应
客户端 (展示结果)
```

---

## 关键代码位置

### 1. 工具定义（参数声明）
**文件：** `server/mcp_server.py` 第486-490行
```python
"n_results": {
    "type": "integer",
    "description": "返回结果数量（默认5）",
    "default": 5
}
```

### 2. 参数接收
**文件：** `server/mcp_server.py` 第591行
```python
n_results = arguments.get("n_results", 5)
```

### 3. 参数传递
**文件：** `server/mcp_server.py` 第600行
```python
results = self.vector_service.search(query, n_results)
```

### 4. 向量搜索执行
**文件：** `server/vector_service.py` 第203-206行
```python
results = self.collection.query(
    query_embeddings=[query_embedding],
    n_results=n_results
)
```

---

## 参数限制

### 客户端限制
- **最小值：** 1
- **最大值：** 20（前端输入框限制）
- **默认值：** 5

### 服务端限制
- **无硬性限制**：ChromaDB 理论上可以返回任意数量的结果
- **实际限制**：受数据库中文档总数限制
- **性能考虑**：返回过多结果可能影响性能

---

## 搜索算法

### 相似度计算
ChromaDB 使用以下方法计算相似度：

1. **余弦相似度（默认）**
   - 计算查询向量与文档向量的夹角
   - 值范围：-1 到 1（1 表示完全相同）
   - 转换为距离：`distance = 1 - cosine_similarity`

2. **欧氏距离（可选）**
   - 计算向量之间的欧氏距离
   - 距离越小，相似度越高

### 排序规则
- 按相似度从高到低排序
- 距离值越小，排名越靠前
- 返回前 `n_results` 个结果

---

## 示例

### 请求示例
```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "tools/call",
  "params": {
    "name": "search_documents",
    "arguments": {
      "query": "劳动合同纠纷",
      "n_results": 10
    }
  }
}
```

### 响应示例
```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "content": [{
      "type": "text",
      "text": "[{\"id\": \"doc1_chunk_0\", \"document\": \"...\", \"metadata\": {...}, \"distance\": 0.15}, ...]"
    }]
  }
}
```

---

## 总结

1. **参数来源**：用户在前端输入框设置（1-20，默认5）
2. **参数传递**：客户端 → MCP服务端 → 向量服务 → ChromaDB
3. **执行位置**：ChromaDB 的 `query()` 方法根据 `n_results` 参数返回最相似的文档
4. **返回结果**：按相似度从高到低排序，返回前 `n_results` 个结果
5. **无额外处理**：服务端直接将参数传递给 ChromaDB，不做额外过滤或处理











