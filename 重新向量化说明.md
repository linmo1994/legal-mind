# 重新向量化说明

## 为什么需要清空并重新向量化？

### 问题原因

1. **向量归一化不一致**：
   - 现有向量是使用旧代码生成的（**未归一化**）
   - 从日志可以看到，distance值很大（8.78, 5.80, 5.57），说明是L2距离且未归一化
   - 新代码会生成**归一化的向量**（normalize_embeddings=True）

2. **距离计算不准确**：
   - 归一化向量和未归一化向量混在一起，会导致距离计算不准确
   - 这会导致检索结果不相关，相似度计算错误

3. **相似度值异常**：
   - 未归一化的L2距离可能很大（>2）
   - 无法准确转换为相似度百分比

## 解决方案

### 方法1：清空向量库（推荐）

使用 `clear_vector_db` 工具清空所有向量，然后重新向量化文档。

**步骤：**

1. **调用清空工具**：
   ```bash
   curl -X POST http://localhost:8000/ \
     -H "Content-Type: application/json" \
     -d '{
       "jsonrpc": "2.0",
       "id": 1,
       "method": "tools/call",
       "params": {
         "name": "clear_vector_db",
         "arguments": {}
       }
     }'
   ```

2. **或者在向量化页面**：
   - 打开 `http://localhost:8888/vectorize.html`
   - 可以通过MCP工具调用 `clear_vector_db`

3. **重新向量化所有文档**：
   - 使用 `vectorize_document` 工具重新向量化所有文档
   - 新向量会自动归一化，确保一致性

### 方法2：重置向量库（完全重置）

使用 `reset_vector_db` 工具删除并重新创建集合。

**步骤：**

1. **调用重置工具**：
   ```bash
   curl -X POST http://localhost:8000/ \
     -H "Content-Type: application/json" \
     -d '{
       "jsonrpc": "2.0",
       "id": 1,
       "method": "tools/call",
       "params": {
         "name": "reset_vector_db",
         "arguments": {}
       }
     }'
   ```

2. **重新向量化所有文档**

### 方法3：手动删除数据文件

如果向量库数据存储在 `./chroma_db` 或 `server/chroma_db`：

```bash
# 停止MCP服务器
# 删除数据文件
rm -rf server/chroma_db/*
# 或者
rm -rf chroma_db/*
# 重新启动服务器，集合会自动创建
```

## 验证重新向量化是否成功

重新向量化后，可以通过以下方式验证：

1. **检查向量归一化状态**：
   - 搜索测试查询
   - 查看日志文件 `.cursor/debug.log`
   - 检查 `embedding_norm` 是否接近 1.0（归一化向量）

2. **检查distance值范围**：
   - 归一化向量的L2距离应该在 0-2 之间
   - 如果distance值仍然很大（>2），说明向量可能未归一化

3. **测试检索相关性**：
   - 搜索 "劳动合同法第52条"
   - 检查返回的文档是否相关
   - 相似度值应该合理（0-100%之间）

## 注意事项

1. **备份数据**（可选）：
   - 如果向量库中有重要数据，建议先备份
   - 备份 `chroma_db` 目录

2. **文档内容保留**：
   - 清空向量库只会删除向量数据
   - 原始文档内容不会被删除（如果存储在其他地方）

3. **重新向量化时间**：
   - 根据文档数量，重新向量化可能需要一些时间
   - 建议分批向量化，避免超时

4. **向量归一化**：
   - 新代码已经自动启用向量归一化（`normalize_embeddings=True`）
   - 无需额外配置

## 相关工具

- `clear_vector_db`：清空所有向量
- `reset_vector_db`：重置集合（删除并重新创建）
- `vectorize_document`：向量化文档（现在会自动归一化）
- `get_vector_db_info`：查看向量库信息（文档数量等）

## 总结

**是的，需要清空所有向量库数据并重新向量化**，因为：

1. ✅ 现有向量未归一化，新向量已归一化
2. ✅ 混合使用会导致距离计算不准确
3. ✅ 检索结果不相关，相似度值异常
4. ✅ 重新向量化后，所有向量都会归一化，确保一致性

重新向量化后，检索结果的相关性和相似度计算应该会显著改善。





