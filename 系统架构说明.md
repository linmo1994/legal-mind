# LegalMind 智能体系统架构说明

## 一、整体架构概览

LegalMind 智能体系统采用**前后端分离**的架构，基于**MCP（Model Context Protocol）协议**构建，主要包含以下层次：

```
┌─────────────────────────────────────────────────────────────┐
│                      前端层 (Web Client)                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │ MCP客户端    │  │ 功能页面     │  │ 向量化界面   │     │
│  │ (mcp_client) │  │ (各种HTML)   │  │ (vectorize)  │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
└─────────────────────────────────────────────────────────────┘
                            ↕ HTTP/WebSocket
┌─────────────────────────────────────────────────────────────┐
│                      服务层 (Server Layer)                    │
│  ┌──────────────┐              ┌──────────────┐            │
│  │ HTTP服务器   │              │ MCP服务器     │            │
│  │ (端口8888)   │              │ (端口8000)    │            │
│  └──────────────┘              └──────────────┘            │
└─────────────────────────────────────────────────────────────┘
                            ↕
┌─────────────────────────────────────────────────────────────┐
│                  业务服务层 (Business Services)              │
│  ┌──────────────┐              ┌──────────────┐            │
│  │ 会话管理服务  │              │ 向量化服务    │            │
│  │SessionService│              │VectorService │            │
│  └──────────────┘              └──────────────┘            │
└─────────────────────────────────────────────────────────────┘
                            ↕
┌─────────────────────────────────────────────────────────────┐
│                  数据存储层 (Data Storage)                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │ SQLite       │  │ ChromaDB     │  │ 外部LLM API  │     │
│  │ (会话数据)   │  │ (向量数据)   │  │ (DeepSeek)   │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
└─────────────────────────────────────────────────────────────┘
```

## 二、核心服务组件

### 1. HTTP服务器 (http_server.py)
**职责：**
- 提供静态文件服务（HTML、CSS、JS）
- 解决浏览器CORS限制
- 支持配置文件（config.json）加载

**技术栈：**
- Python标准库 `http.server`
- 端口：8888

**主要功能：**
- 静态资源托管
- CORS头设置
- 目录服务

---

### 2. MCP服务器 (mcp_server.py)
**职责：**
- 实现MCP协议标准
- 提供资源和工具接口
- LLM API代理服务
- 会话管理API

**技术栈：**
- Python标准库 `http.server`
- 多线程HTTP服务器
- 端口：8000

**主要功能模块：**

#### 2.1 MCP协议处理
- **初始化握手** (`/initialize`): 建立MCP连接，交换能力信息
- **资源列表** (`/resources/list`): 提供可用资源列表
- **资源读取** (`/resources/read`): 读取具体资源内容
- **工具列表** (`/tools/list`): 提供可用工具列表
- **工具调用** (`/tools/call`): 执行工具调用
- **提示词列表** (`/prompts/list`): 提供提示词模板列表
- **提示词获取** (`/prompts/get`): 获取具体提示词模板

#### 2.2 资源类型
系统提供以下MCP资源：

1. **法律文书模板** (`legal://doc_template`)
   - 民间借贷纠纷起诉状
   - 离婚协议书
   - 劳动合同
   - 房屋租赁合同
   - 等

2. **法律法规检索** (`legal://law_regulation`)
   - 支持查询民法典、合同法等法律法规

3. **类案检索** (`legal://similar_cases`)
   - 基于向量相似度搜索相似案例

4. **合同审查规则** (`legal://contract_review_rules`)
   - 提供合同审查的规则和指南

#### 2.3 LLM代理服务
- **接口**: `/api/llm/chat`
- **功能**: 代理客户端请求，转发到DeepSeek API
- **特性**: 
  - 流式响应（SSE）
  - 错误处理和重试
  - 请求日志记录

#### 2.4 会话管理API
- `GET /api/sessions` - 获取会话列表
- `GET /api/sessions/{session_id}` - 获取会话详情
- `POST /api/sessions` - 创建新会话
- `POST /api/sessions/{session_id}` - 更新会话
- `POST /api/sessions/{session_id}/messages` - 添加消息
- `DELETE /api/sessions/{session_id}` - 删除会话

---

### 3. 会话管理服务 (session_service.py)
**职责：**
- 管理用户会话状态
- 存储对话历史
- 维护会话元数据

**技术栈：**
- SQLite数据库
- 数据库文件：`sessions.db`

**数据模型：**

**sessions表：**
- `session_id`: 会话唯一标识
- `title`: 会话标题
- `status`: 会话状态（active/completed/expired）
- `current_intent`: 当前意图
- `collected_parameters`: 已收集的参数（JSON）
- `missing_parameters`: 缺失的参数（JSON）
- `stage`: 当前阶段
- `context_cache`: 上下文缓存（JSON）
- `created_at`: 创建时间
- `updated_at`: 更新时间
- `last_user_input`: 最后用户输入
- `last_user_input_time`: 最后输入时间

**messages表：**
- `id`: 消息ID（自增）
- `session_id`: 所属会话ID
- `role`: 角色（user/assistant/system）
- `content`: 消息内容
- `timestamp`: 时间戳

**主要方法：**
- `create_session()` - 创建新会话
- `get_session()` - 获取会话
- `update_session()` - 更新会话
- `delete_session()` - 删除会话
- `add_message()` - 添加消息
- `get_messages()` - 获取消息历史
- `list_sessions()` - 列出所有会话

---

### 4. 向量化服务 (vector_service.py)
**职责：**
- 文档向量化
- 向量相似度搜索
- 向量数据持久化

**技术栈：**
- ChromaDB（向量数据库）
- SentenceTransformers（嵌入模型）
- 模型：`paraphrase-multilingual-MiniLM-L12-v2`
- 数据库目录：`chroma_db/`

**主要功能：**

1. **文档向量化**
   - 将法律文档转换为384维向量
   - 支持中文文本处理

2. **相似文档搜索**
   - 基于向量相似度查找相似案例
   - 支持Top-K检索

3. **向量存储**
   - 持久化到本地文件系统
   - 支持增量添加文档

**主要方法：**
- `add_document()` - 添加文档到向量库
- `search_similar()` - 搜索相似文档
- `get_document()` - 获取文档内容
- `delete_document()` - 删除文档

---

## 三、前端组件

### 1. MCP客户端 (mcp_client.html/js)
**职责：**
- 实现MCP协议客户端
- 与MCP服务器通信
- 与LLM交互
- 用户界面展示

**主要功能：**
- MCP协议握手
- 资源列表获取
- 工具调用
- LLM请求发送和响应处理
- 会话管理
- 参数收集界面
- 思考内容和结论分离展示

### 2. 功能页面
- `index.html` - 主入口页面
- `case_input.html` - 案件录入
- `ai_analysis.html` - AI分析
- `judgment.html` - 判决结果
- `evidence.html` - 证据管理
- `history.html` - 历史记录
- `vectorize.html` - 向量化文档

---

## 四、数据流

### 典型请求流程：

```
1. 用户输入
   ↓
2. 客户端构建请求数据
   - 系统提示词
   - 资源列表
   - 会话状态
   - 对话历史
   ↓
3. 客户端发送到 MCP服务器 /api/llm/chat
   ↓
4. MCP服务器代理转发到 DeepSeek API
   ↓
5. LLM返回流式响应
   ↓
6. MCP服务器解析并格式化响应
   ↓
7. 客户端接收并解析响应
   - 判断响应类型（type字段）
   - 参数不足 → 显示参数收集界面
   - 需要调用资源 → 调用MCP资源接口
   - 生成结果 → 显示最终结果
   ↓
8. 更新会话状态和历史记录
```

### 资源调用流程：

```
1. LLM判断需要调用资源
   ↓
2. 客户端调用 MCP服务器 /resources/read
   ↓
3. MCP服务器返回资源内容
   ↓
4. 客户端将资源内容加入上下文
   ↓
5. 再次调用LLM生成最终结果
```

---

## 五、外部依赖

### 1. DeepSeek LLM API
- **API地址**: `https://api.deepseek.com/v1/chat/completions`
- **模型**: `deepseek-chat`
- **用途**: 智能分析和内容生成

### 2. ChromaDB
- **类型**: 向量数据库
- **用途**: 存储和检索文档向量
- **存储**: 本地文件系统

### 3. SQLite
- **用途**: 会话和消息数据存储
- **文件**: `sessions.db`

---

## 六、配置文件

### config.json
```json
{
  "mcp_server": {
    "host": "localhost",
    "port": 8000
  },
  "llm": {
    "api_url": "https://api.deepseek.com/v1/chat/completions",
    "api_key": "...",
    "model": "deepseek-chat",
    "timeout": 60000,
    "max_retries": 3,
    "temperature": 0.0,
    "max_tokens": 2048
  },
  "system_prompt": "..."
}
```

---

## 七、服务启动

### 启动顺序：
1. **MCP服务器** (端口8000)
   ```bash
   python3 server/mcp_server.py
   ```

2. **HTTP服务器** (端口8888)
   ```bash
   python3 server/http_server.py 8888
   ```

3. **访问应用**
   ```
   http://localhost:8888/index.html
   ```

---

## 八、技术特点

1. **协议标准化**: 基于MCP协议，易于扩展
2. **模块化设计**: 服务职责清晰，易于维护
3. **向量化支持**: 支持相似案例检索
4. **会话管理**: 完整的会话状态管理
5. **流式响应**: 支持LLM流式输出
6. **错误处理**: 完善的异常处理机制

---

## 九、扩展性

系统设计支持以下扩展：
- 新增资源类型
- 新增工具功能
- 新增提示词模板
- 集成其他LLM提供商
- 支持多用户和权限管理
- 数据库迁移（从SQLite到PostgreSQL等）








