# 客户端初始化握手流程说明

## 概述

客户端在初始化时**确实有**与服务端进行握手和能力发现的流程。这是MCP协议的标准初始化流程。

## 初始化流程

### 1. 入口函数

**位置**: `mcp_client.js` 第211行（`init` 函数）

**流程**:
```
页面加载
    ↓
init() (第211行)
    ↓
loadConfig() (第215行) - 加载配置文件
    ↓
initializeMCP() (第243行) - MCP初始化握手
```

### 2. MCP初始化握手

**位置**: `mcp_client.js` 第452行（`initializeMCP` 函数）

**步骤**:

#### 步骤1: 发送初始化请求

```javascript
const initResponse = await sendMCPRequest({
  method: 'initialize',
  params: {
    protocolVersion: '2024-11-05',
    capabilities: {},
    clientInfo: {
      name: 'LegalMind 客户端',
      version: '1.0.0'
    }
  }
});
```

**请求内容**:
- `method`: `'initialize'` - MCP协议初始化方法
- `protocolVersion`: `'2024-11-05'` - MCP协议版本
- `capabilities`: `{}` - 客户端能力声明（当前为空对象）
- `clientInfo`: 客户端信息（名称和版本）

#### 步骤2: 处理初始化响应

```javascript
if (initResponse.error) {
  throw new Error(initResponse.error.message);
}

if (!initResponse.result) {
  throw new Error('MCP初始化响应格式错误');
}

console.log('协议版本:', initResponse.result.protocolVersion);
```

**响应内容**:
- `protocolVersion`: 服务端支持的协议版本
- `capabilities`: 服务端能力声明
- `serverInfo`: 服务端信息（名称、版本等）

#### 步骤3: 解析服务端能力声明

```javascript
const capabilities = initResponse.result.capabilities || {};

// 检查资源支持
if (capabilities.resources !== undefined) {
  console.log(`  ✅ Resources: ${capabilities.resources ? '支持' : '不支持'}`);
  if (capabilities.resources) {
    console.log('     - 可以调用 resources/list');
    console.log('     - 可以调用 resources/read');
  }
}

// 检查提示词支持
if (capabilities.prompts !== undefined) {
  console.log(`  ✅ Prompts: ${capabilities.prompts ? '支持' : '不支持'}`);
  if (capabilities.prompts) {
    console.log('     - 可以调用 prompts/list');
  }
}

// 检查工具支持
if (capabilities.tools !== undefined) {
  console.log(`  ✅ Tools: ${capabilities.tools ? '支持' : '不支持'}`);
  if (capabilities.tools) {
    console.log('     - 可以调用 tools/list');
  }
}
```

**能力类型**:
- `resources`: 资源支持（如法律文档模板、法律法规等）
- `prompts`: 提示词支持
- `tools`: 工具支持

#### 步骤4: 发送初始化完成通知

```javascript
await sendMCPRequest({
  method: 'notifications/initialized'
});
```

**说明**: 通知服务端客户端已完成初始化，可以开始正常通信。

### 3. 能力发现

**位置**: `mcp_client.js` 第569行（`initializeMCP` 函数中）

**步骤**:

#### 步骤1: 发现资源列表

```javascript
if (capabilities.resources) {
  const resourcesResponse = await sendMCPRequest({
    method: 'resources/list'
  });
  
  if (resourcesResponse.result && resourcesResponse.result.resources) {
    mcpResources = resourcesResponse.result.resources;
    console.log(`✅ 发现 ${mcpResources.length} 个资源`);
    mcpResources.forEach(resource => {
      console.log(`  - ${resource.uri}: ${resource.name || resource.uri}`);
    });
  }
}
```

**获取内容**:
- 资源URI列表
- 资源名称和描述
- 资源参数定义

#### 步骤2: 发现工具列表

```javascript
if (capabilities.tools) {
  const toolsResponse = await sendMCPRequest({
    method: 'tools/list'
  });
  
  if (toolsResponse.result && toolsResponse.result.tools) {
    const tools = toolsResponse.result.tools;
    console.log(`✅ 发现 ${tools.length} 个工具`);
    tools.forEach(tool => {
      console.log(`  - ${tool.name}: ${tool.description || '无描述'}`);
    });
  }
}
```

**获取内容**:
- 工具名称列表
- 工具描述
- 工具参数定义

#### 步骤3: 发现提示词列表

```javascript
if (capabilities.prompts) {
  const promptsResponse = await sendMCPRequest({
    method: 'prompts/list'
  });
  
  if (promptsResponse.result && promptsResponse.result.prompts) {
    mcpPrompts = promptsResponse.result.prompts;
    console.log(`✅ 发现 ${mcpPrompts.length} 个提示词`);
    mcpPrompts.forEach(prompt => {
      console.log(`  - ${prompt.name}: ${prompt.description || '无描述'}`);
    });
  }
}
```

**获取内容**:
- 提示词名称列表
- 提示词描述
- 提示词参数定义

## 完整初始化流程

```
页面加载
    ↓
init() (第211行)
    ↓
loadConfig() - 加载配置文件
    ↓
initializeMCP() (第452行)
    ↓
【步骤1】发送 initialize 请求
    ├─ 协议版本: '2024-11-05'
    ├─ 客户端能力: {}
    └─ 客户端信息: { name, version }
    ↓
【步骤2】接收初始化响应
    ├─ 协议版本
    ├─ 服务端能力声明 (capabilities)
    └─ 服务端信息 (serverInfo)
    ↓
【步骤3】解析服务端能力
    ├─ resources: 支持/不支持
    ├─ prompts: 支持/不支持
    └─ tools: 支持/不支持
    ↓
【步骤4】发送 initialized 通知
    └─ notifications/initialized
    ↓
【步骤5】能力发现
    ├─ resources/list (如果支持资源)
    ├─ tools/list (如果支持工具)
    └─ prompts/list (如果支持提示词)
    ↓
初始化完成
```

## 代码位置

- **入口函数**: `mcp_client.js` 第211行（`init` 函数）
- **初始化握手**: `mcp_client.js` 第452行（`initializeMCP` 函数）
- **能力发现**: `mcp_client.js` 第569-650行（在 `initializeMCP` 函数中）

## 关键代码片段

### 初始化请求

```javascript
const initResponse = await sendMCPRequest({
  method: 'initialize',
  params: {
    protocolVersion: '2024-11-05',
    capabilities: {},
    clientInfo: {
      name: 'LegalMind 客户端',
      version: '1.0.0'
    }
  }
});
```

### 能力发现

```javascript
// 发现资源
const resourcesResponse = await sendMCPRequest({
  method: 'resources/list'
});

// 发现工具
const toolsResponse = await sendMCPRequest({
  method: 'tools/list'
});

// 发现提示词
const promptsResponse = await sendMCPRequest({
  method: 'prompts/list'
});
```

## 总结

**客户端在初始化时确实有完整的握手和能力发现流程**：

1. ✅ **初始化握手**: 发送 `initialize` 请求，接收服务端能力声明
2. ✅ **能力发现**: 根据服务端能力声明，调用 `resources/list`、`tools/list`、`prompts/list` 发现可用资源
3. ✅ **初始化完成**: 发送 `notifications/initialized` 通知服务端

这是标准的MCP协议初始化流程，确保客户端和服务端能够正确协商协议版本和能力。






