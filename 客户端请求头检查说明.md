# 客户端请求头检查说明

## 关键点说明

### 1. Content-Type vs Accept
- **Content-Type**: 表示客户端**发送**的数据类型
- **Accept**: 表示客户端**期望接收**的数据类型

### 2. 客户端代码分析

在 `callLLM` 函数中（mcp_client.js 第760-816行）：

```javascript
// 构建请求头
const headers = {
  'Content-Type': 'application/json'  // 发送JSON数据
};

// 如果启用流式输出，添加Accept头声明期望接收text/event-stream
if (stream) {
  headers['Accept'] = 'text/event-stream';  // 期望接收SSE格式
}
```

### 3. 结论

✅ **Content-Type**: 始终是 `application/json`（正确，因为发送的是JSON数据）
✅ **Accept**: 如果是流式模式，设置为 `text/event-stream`（正确，期望接收SSE格式）

### 4. 后续轮次对话

在 `handleUserInput` 函数中，每次调用 `callLLM` 时都会传入 `onStreamChunk` 回调：
- 第一轮对话：`callLLM(requestData, onStreamChunk)` → stream = true
- 后续轮次对话：`callLLM(requestData, onStreamChunk)` → stream = true

所以**所有轮次**（包括后续轮次）都会：
- 设置 `Content-Type: application/json`（发送JSON）
- 设置 `Accept: text/event-stream`（期望接收SSE）

## 验证方法

在浏览器控制台（F12）中查看网络请求，应该看到：
- Request Headers:
  - `Content-Type: application/json`
  - `Accept: text/event-stream`（如果是流式请求）
- Response Headers:
  - `Content-Type: text/event-stream`（服务端返回的）
