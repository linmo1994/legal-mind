# 优化说明：直接使用 requestData 而不是构建 messages 数组

## 当前实现的问题

### 客户端当前逻辑：
1. `buildRequestData()` 构建了完整的 requestData 对象，包含：
   - `system_prompt`: 系统提示词
   - `tools`: 工具列表
   - `resources`: 资源列表
   - `session`: 会话信息
   - `conversation_history`: 对话历史（数组格式）
   - `current_user_input`: 用户输入
   - `config`: 配置信息

2. 然后将 requestData 序列化为 JSON 字符串，构建 messages 数组：
   ```javascript
   const messages = [
     { role: 'system', content: CONFIG.systemPrompt },  // 重复了 system_prompt
     { role: 'user', content: `请根据以下数据分析和回答：\n\n${requestText}` }  // 将整个 requestData 作为字符串
   ];
   ```

### 问题分析：
1. **重复数据**：`system_prompt` 在 requestData 中已经有了，又单独提取了一次
2. **数据格式浪费**：将结构化的 requestData 序列化为字符串，模型需要解析 JSON
3. **历史对话未利用**：`conversation_history` 没有被转换为 messages 格式，而是作为字符串的一部分
4. **职责不清**：客户端需要知道 DeepSeek API 的 messages 格式要求

## 优化方案

### 客户端：
直接发送 requestData 给代理服务，不构建 messages 数组

### 服务端：
根据 requestData 构建 messages 数组：
1. **system 消息**：使用 `requestData.system_prompt`
2. **历史对话**：将 `requestData.conversation_history` 转换为 messages 格式
3. **user 消息**：使用 `requestData.current_user_input`
4. **其他参数**：从 `requestData.config` 中获取 temperature、max_tokens 等

### 优势：
1. **职责分离**：客户端只负责构建业务数据，服务端负责 API 格式转换
2. **利用历史对话**：正确地将历史对话转换为 messages 格式，让模型能够理解上下文
3. **数据不重复**：system_prompt 只传递一次
4. **更灵活**：服务端可以根据需要调整 messages 的构建逻辑











