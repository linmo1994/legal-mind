# 向量化功能接口说明

## 接口类型

### ✅ 向量化功能使用 MCP 协议接口

向量化功能**完全通过 MCP 协议**与服务端通信，**没有提供独立的 HTTP REST API 接口**。

---

## 实现方式

### 1. 客户端调用方式

**位置：** `vectorize.html`

```javascript
const MCP_SERVER_URL = 'http://localhost:8000';

// 发送MCP请求
async function sendMCPRequest(method, params = {}) {
  const request = {
    jsonrpc: '2.0',
    id: generateRequestId(),
    method: method,  // 例如: 'tools/call'
    params: params
  };

  const response = await fetch(MCP_SERVER_URL, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify(request)
  });

  return await response.json();
}

// 调用工具
async function callTool(toolName, arguments_) {
  const result = await sendMCPRequest('tools/call', {
    name: toolName,  // 例如: 'vectorize_document'
    arguments: arguments_
  });
  return JSON.parse(result.content[0].text);
}
```

**说明：**
- 使用标准的 MCP JSON-RPC 协议
- 通过 `tools/call` 方法调用向量化工具
- 请求发送到 `http://localhost:8000`（MCP服务端）

---

### 2. 服务端处理方式

**位置：** `server/mcp_server.py`

#### HTTP服务器包装

```python
class MCPHTTPHandler(BaseHTTPRequestHandler):
    def do_POST(self):
        path = self.path.split('?')[0]
        
        # 特殊接口：LLM代理（非MCP协议）
        if path == '/api/llm/chat':
            self._handle_llm_proxy()
            return
        
        # 其他请求：转换为MCP协议请求
        request = json.loads(post_data.decode('utf-8'))
        response = self.server_instance.handle_request(request)
        # 返回MCP协议响应
```

#### MCP工具定义

```python
tools.append({
    "name": "vectorize_document",
    "description": "将文档向量化并存储到ChromaDB中",
    "inputSchema": {
        "type": "object",
        "properties": {
            "document_id": {"type": "string"},
            "text": {"type": "string"},
            "metadata": {"type": "object"}
        },
        "required": ["document_id", "text"]
    }
})
```

#### 工具调用处理

```python
def _handle_tool_call(self, request_id, params):
    tool_name = params.get("name")
    arguments = params.get("arguments", {})
    
    if tool_name == "vectorize_document":
        result = self.vector_service.add_document(...)
        return {
            "jsonrpc": "2.0",
            "id": request_id,
            "result": {
                "content": [{
                    "type": "text",
                    "text": json.dumps(result)
                }]
            }
        }
```

---

## 服务端提供的接口

### 1. MCP协议接口（主要接口）

**端点：** `POST http://localhost:8000`

**协议：** JSON-RPC 2.0（MCP协议）

**支持的MCP方法：**
- `initialize` - 初始化MCP连接
- `tools/list` - 获取工具列表
- `tools/call` - 调用工具（包括向量化工具）
- `resources/list` - 获取资源列表
- `resources/read` - 读取资源
- `prompts/list` - 获取提示词模板列表
- `prompts/get` - 获取提示词模板
- `notifications/initialized` - 初始化通知

**向量化相关工具：**
- `vectorize_document` - 向量化文档
- `search_documents` - 搜索相似文档
- `delete_document` - 删除文档
- `get_vector_db_info` - 获取数据库信息

---

### 2. 非MCP协议接口（辅助接口）

#### 2.1 LLM代理接口

**端点：** `POST http://localhost:8000/api/llm/chat`

**用途：** 代理LLM API请求，解决CORS问题

**请求格式：**
```json
{
  "model": "deepseek-chat",
  "messages": [...],
  "temperature": 0.0,
  "max_tokens": 2048
}
```

**说明：** 这是**唯一一个非MCP协议的接口**，用于转发LLM请求。

---

#### 2.2 健康检查接口

**端点：** `GET http://localhost:8000/health`

**用途：** 服务健康检查

**响应：**
```json
{"status": "ok"}
```

---

## 总结

### ✅ 向量化功能使用 MCP 协议

1. **完全基于MCP协议**：向量化功能通过 `tools/call` 方法调用
2. **没有独立的REST API**：没有提供类似 `/api/vectorize` 这样的独立接口
3. **统一协议**：所有功能（包括向量化）都通过MCP协议统一管理

### 服务端提供的接口类型

| 接口类型 | 端点 | 协议 | 用途 |
|---------|------|------|------|
| **MCP协议** | `POST /` | JSON-RPC 2.0 | 所有MCP功能（包括向量化） |
| **LLM代理** | `POST /api/llm/chat` | HTTP JSON | 代理LLM API请求 |
| **健康检查** | `GET /health` | HTTP JSON | 服务健康检查 |

### 设计优势

1. **统一协议**：所有功能使用统一的MCP协议，便于管理和扩展
2. **标准化**：遵循MCP标准，易于与其他MCP客户端集成
3. **工具化**：向量化功能作为MCP工具，可以与其他工具组合使用

---

## 代码位置

- **客户端调用：** `vectorize.html` 第310-360行
- **服务端处理：** `server/mcp_server.py` 第406行（tools/call）、第558行（vectorize_document）
- **向量服务：** `server/vector_service.py`











