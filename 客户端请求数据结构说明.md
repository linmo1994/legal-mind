# 客户端请求数据结构说明

## 概述

当用户点击发送按钮后，客户端会调用 `buildRequestData()` 函数构建请求数据，然后通过 `callLLM()` 函数发送到 LLM 代理服务。

## 请求数据构建流程

### 1. 触发点

用户点击发送按钮 → `handleUserInput()` → `buildRequestData(userInput)` → `callLLM(requestData)`

### 2. buildRequestData() 函数

位置：`mcp_client.js:1439`

**功能**：组装发送给 LLM 代理服务的请求数据

## 请求数据结构

### 完整请求对象（重构后）

```javascript
{
  // 系统提示词（从 CONFIG.systemPrompt 获取）
  "system_prompt": "你是一名 AI 法律智能体，精通法律实务...",
  
  // 统一的服务端能力字段（重构后）
  "server_ability": {
    // MCP 工具列表（从 mcpTools 全局变量获取）
    "tools": [
      {
        "name": "工具名称",
        "description": "工具描述",
        "inputSchema": {
          // 工具输入参数定义
        }
      }
    ],
    
    // MCP 资源列表（从 mcpResources 全局变量获取）
    "resources": [
      {
        "uri": "legal://doc_template",
        "description": "资源描述",
        "parameters": {
          "type": "object",
          "properties": {
            "template_name": {
              "type": "string",
              "description": "必填，法律文书模板名称"
            }
          },
          "required": ["template_name"]
        }
      }
    ],
    
    // MCP 提示词模板列表（从 mcpPrompts 全局变量获取）
    "prompts": [
      {
        "name": "judge_work_guide",
        "description": "裁判者工作指南：指导 LegalMind ..."
      }
    ]
  },
  
  // 统一的用户输入字段（重构后）
  "user_input": {
    // 用户输入的文本
    "text": "请你作为裁判者帮我断案",
    
    // 文件ID列表（可选，如果有上传的文件）
    "file_ids": [
      "file_123",
      "file_456"
    ]
  },
  
  // 会话信息
  "session": {
    "session_id": "session_xxx",
    "status": "active",
    "current_intent": "法官断案",
    "collected_parameters": {},
    "missing_parameters": [],
    "stage": "waiting_for_input",
    "context_cache": {},
    "created_at": "2025-01-08T10:00:00.000Z",
    "updated_at": "2025-01-08T10:05:00.000Z"
  },
  
  // 对话历史记录
  "conversation_history": [
    {
      "role": "user",
      "content": "用户之前的问题"
    },
    {
      "role": "assistant",
      "content": "AI之前的回答"
    }
  ]
}
```

### 旧格式（向后兼容）

服务端同时支持旧格式，以保持向后兼容：

```javascript
{
  "system_prompt": "...",
  "tools": [...],           // 旧格式
  "resources": [...],       // 旧格式
  "prompts": [...],         // 旧格式
  "current_user_input": "...", // 旧格式
  "file_ids": [...],        // 旧格式
  "session": {...},
  "conversation_history": [...]
}
```

## 字段详细说明

### 1. system_prompt

- **类型**：`String`
- **来源**：`CONFIG.systemPrompt`（从 `config.json` 加载）
- **说明**：系统提示词，定义 AI 的角色、工作流程和输出格式

### 2. server_ability（重构后）

- **类型**：`Object`
- **说明**：统一的服务端能力字段，包含工具、资源和提示词模板

#### 2.1 server_ability.tools

- **类型**：`Array<Object>`
- **来源**：`mcpTools` 全局变量（通过 MCP `tools/list` 发现）
- **结构**：
  ```javascript
  {
    "name": "工具名称",
    "description": "工具描述",
    "inputSchema": {
      // 工具输入参数定义（JSON Schema）
    }
  }
  ```
- **说明**：MCP 工具列表，用于告诉 LLM 有哪些工具可用

#### 2.2 server_ability.resources

- **类型**：`Array<Object>`
- **来源**：`mcpResources` 全局变量（通过 MCP `resources/list` 发现）
- **结构**：
  ```javascript
  {
    "uri": "legal://doc_template",
    "description": "资源描述",
    "parameters": {
      // 资源参数定义（JSON Schema）
    }
  }
  ```
- **说明**：MCP 资源列表，用于告诉 LLM 有哪些资源可用

#### 2.3 server_ability.prompts

- **类型**：`Array<Object>`
- **来源**：`mcpPrompts` 全局变量（通过 MCP `prompts/list` 发现）
- **结构**：
  ```javascript
  {
    "name": "judge_work_guide",
    "description": "提示词模板描述"
  }
  ```
- **说明**：MCP 提示词模板列表，用于告诉 LLM 有哪些提示词模板可用

### 3. user_input（重构后）

- **类型**：`Object`
- **说明**：统一的用户输入字段，包含文本和文件

#### 3.1 user_input.text

- **类型**：`String`
- **来源**：用户输入的文本
- **说明**：当前用户的问题或请求

#### 3.2 user_input.file_ids

- **类型**：`Array<String>`（可选）
- **来源**：
  - `window.uploadedFileIds`（从首页上传的文件）
  - `pendingFiles`（当前页面上传的文件）
- **说明**：已上传到服务端的文件ID列表

### 4. session

- **类型**：`Object`
- **来源**：`currentSession` 全局变量
- **字段**：
  - `session_id`: 会话ID
  - `status`: 会话状态（如 "active"）
  - `current_intent`: 当前意图（如 "法官断案"）
  - `collected_parameters`: 已收集的参数
  - `missing_parameters`: 缺失的参数
  - `stage`: 当前阶段（如 "waiting_for_input"）
  - `context_cache`: 上下文缓存
  - `created_at`: 创建时间
  - `updated_at`: 更新时间

### 5. conversation_history

- **类型**：`Array<Object>`
- **来源**：`currentSession.conversationHistory`
- **结构**：
  ```javascript
  [
    {
      "role": "user",
      "content": "用户消息"
    },
    {
      "role": "assistant",
      "content": "AI回复"
    }
  ]
  ```
- **说明**：历史对话记录，用于上下文理解

## 请求发送

### callLLM() 函数

位置：`mcp_client.js:1088`

**请求方式**：`POST`
**请求URL**：`http://localhost:8000/api/llm/chat`
**请求头**：
```javascript
{
  'Content-Type': 'application/json',
  'Accept': 'text/event-stream'  // 如果启用流式输出
}
```

**请求体**：
```javascript
{
  ...requestData,  // 上述完整的请求数据
  "stream": true   // 是否启用流式输出
}
```

## 数据来源总结

| 字段 | 来源 | 说明 |
|------|------|------|
| `system_prompt` | `CONFIG.systemPrompt` | 从 `config.json` 加载 |
| `server_ability.tools` | `mcpTools` | 通过 MCP `tools/list` 发现 |
| `server_ability.resources` | `mcpResources` | 通过 MCP `resources/list` 发现 |
| `server_ability.prompts` | `mcpPrompts` | 通过 MCP `prompts/list` 发现 |
| `user_input.text` | 用户输入 | 文本输入框内容 |
| `user_input.file_ids` | `window.uploadedFileIds` + `pendingFiles` | 上传的文件ID |
| `session` | `currentSession` | 会话管理对象 |
| `conversation_history` | `currentSession.conversationHistory` | 会话历史 |

## 关键点

1. **动态能力发现**：`tools`、`resources`、`prompts` 都是通过 MCP 协议动态发现的，不是硬编码
2. **会话状态管理**：`session` 对象包含完整的会话状态信息
3. **上下文传递**：`conversation_history` 提供对话上下文
4. **文件支持**：`file_ids` 支持多文件上传场景

## 示例请求

### 首次请求（无历史记录）

```json
{
  "system_prompt": "你是一名 AI 法律智能体...",
  "server_ability": {
    "tools": [],
    "resources": [
      {
        "uri": "legal://doc_template",
        "description": "法律文书模板",
        "parameters": {...}
      }
    ],
    "prompts": [
      {
        "name": "judge_work_guide",
        "description": "法官工作指南"
      }
    ]
  },
  "user_input": {
    "text": "请你作为裁判者帮我断案"
  },
  "session": {
    "session_id": "session_123",
    "status": "active",
    "current_intent": null,
    "collected_parameters": {},
    "missing_parameters": [],
    "stage": "waiting_for_input",
    "context_cache": {},
    "created_at": "2025-01-08T10:00:00.000Z",
    "updated_at": "2025-01-08T10:05:00.000Z"
  },
  "conversation_history": []
}
```

### 后续请求（有历史记录）

```json
{
  "system_prompt": "你是一名 AI 法律智能体...",
  "server_ability": {
    "tools": [],
    "resources": [...],
    "prompts": [...]
  },
  "user_input": {
    "text": "案件是这样的：张三借给李四10万元...",
    "file_ids": ["file_123"]
  },
  "session": {
    "session_id": "session_123",
    "status": "active",
    "current_intent": "法官断案",
    "collected_parameters": {},
    "missing_parameters": [],
    "stage": "generate_with_llm",
    "context_cache": {},
    "created_at": "2025-01-08T10:00:00.000Z",
    "updated_at": "2025-01-08T10:10:00.000Z"
  },
  "conversation_history": [
    {
      "role": "user",
      "content": "请你作为裁判者帮我断案"
    },
    {
      "role": "assistant",
      "content": "好的，我将作为法官帮您分析案件..."
    }
  ]
}
```
