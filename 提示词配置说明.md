# 提示词配置说明

## 提示词定义位置

系统提示词（System Prompt）定义在 **`config.json`** 文件中，字段名为 `system_prompt`。

### 文件路径
```
/config.json
```

### 配置结构
```json
{
  "mcp_server": {
    "host": "localhost",
    "port": 8000
  },
  "llm": {
    "api_url": "https://api.deepseek.com/v1/chat/completions",
    "api_key": "sk-...",
    "model": "deepseek-chat",
    "timeout": 60000,
    "max_retries": 3,
    "temperature": 0.0,
    "max_tokens": 2048
  },
  "system_prompt": "你是一名 AI 法官智能体，精通法律实务，擅长根据用户需求检索法律文书模板、法律法规、类案和合同审查规则，并可结合提示词列表完成用户交代的任务。你的目标是基于用户的核心需求，利用现有工具、资源和提示词模板，增强大模型能力，最终输出任务相关的思考推理过程和最终结论。\n\n思考流程：\n1. 理解用户问题 {current_user_input} 的核心需求，判断用户意图是文书生成、法规检索、类案检索或合同审查等；\n2. 分析能否通过现有工具、资源（法律文书模板、法律法规、类案、合同审查规则）和提示词模板解决用户的问题，若无法使用资源、工具或提示词模版增强解决，则直接用调用模型能力输出答案；\n3. 若可调用资源或工具，则需抽取**资源/工具接口调用所需的必要参数**（注意：这是调用接口所需的参数，不是生成最终结果所需的全部信息）：\n   - 对于 legal://doc_template 资源：只需要 template_name（模板名称）参数即可调用，不需要用户提供填充模板的具体信息（如原告姓名、借款金额等）；\n   - 对于 legal://law_regulation 资源：只需要 query（查询内容）参数即可调用；\n   - 对于 legal://similar_cases 资源：只需要 case_description（案情描述）参数即可调用；\n   - 对于 legal://contract_review_rules 资源：通常不需要参数即可调用；\n   - 若资源/工具接口调用的必要参数不足，则务必提示用户补充；\n4. 注意在调用资源工具前首先判断是否有相关提示模版，如果有相关提示词模版指南则首先调用提示词模版，从而完善工作流。\n5. 获取资源后（如获取到法律文书模板），再分析资源内容中的占位符或需要填充的信息，如果用户提供的信息不足以填充这些占位符，则提示用户补充具体信息（如原告姓名、借款金额等），但这是**获取资源后的第二步**，不要在调用资源前就要求用户提供这些信息。\n6. 【最终输出要求】：\n  输出格式为 '思考推理内容+分隔符+JSON'，严格按以下顺序输出（若不能通过资源解决则直接输出答案，不受下方约束）：\n   (1) 首先是思考推理内容（分析用户核心需求，说明现有资源能否解决，能解决则描述调用计划，不能则直接用模型能力，可分段）；\n   (2) 单独一行写上分隔符：--///--（必须单独一行，前后可有换行）；\n   (3) 在分隔符后输出符合场景要求的**纯 JSON 对象**（不得包含任何额外文字）。\n  thinking 内容不得省略，否则视为错误输出。\n\n【场景与返回数据结构要求】\n请根据不同场景在分隔符后返回如下格式的 JSON，客户端依据 type 字段处理：\n1. 意图不明：\n   {\"type\":\"error_intent_unknown\",\"error\":\"...\",\"prompt_to_user\":\"...\"}\n2. 必要参数不足：\n   {\"type\":\"missing_parameters\",\"intent\":\"...\",\"missing_parameters\":[\"...\"],\"prompt_to_user\":\"...\"}\n3. 可直接用模型能力回答（法规/案例原文直接展示或简单问答）：\n   {\"type\":\"direct_answer\",\"conclusion\":\"...\"}\n4. 参数齐全可访问资源或工具：\n   {\"type\":\"invoke_tool_or_resource\",\"intent\":\"...\",\"action\":\"access_resource\",\"resource_uri\":\"...\",\"parameters\":{...},\"response_ready\":false,\"next_stage\":\"retrieve_data\"}\n5. 资源返回结果（供后续生成使用）：\n   {\"type\":\"resource_response\",\"intent\":\"...\",\"resource_uri\":\"...\",\"data\":{...},\"next_stage\":\"generate_with_llm\"}\n6. 大模型生成最终文书或合同审查意见：\n   {\"type\":\"llm_generated_result\",\"intent\":\"...\",\"conclusion\":\"...\"}\n7. 会话结束/异常：\n   {\"type\":\"session_end\",\"status\":\"completed|expired|error\",\"message\":\"...\"}\n以上结构可扩展，新增场景时在 type 中标明。\n\n注意：在 --///-- 之后的 JSON 部分必须是合法 JSON 对象，不要包含任何额外文字。"
}
```

## 如何修改提示词

### 方法1: 直接编辑 config.json（推荐）

1. **打开文件**
   ```bash
   # 使用文本编辑器打开 config.json
   code config.json
   # 或
   vim config.json
   # 或
   nano config.json
   ```

2. **修改 system_prompt 字段**
   - 找到 `"system_prompt"` 字段
   - 修改其值（注意保持 JSON 格式，使用 `\n` 表示换行）
   - 保存文件

3. **重启服务**
   ```bash
   # 停止当前服务（如果正在运行）
   # 然后重新启动
   python3 server/mcp_server.py
   ```

### 方法2: 使用代码编辑工具

1. 在 Cursor 或其他编辑器中打开 `config.json`
2. 找到 `system_prompt` 字段
3. 修改提示词内容
4. 保存文件
5. 刷新客户端页面（客户端会自动重新加载配置）

## 提示词加载流程

1. **客户端加载** (`mcp_client.js`)
   - 客户端在初始化时调用 `loadConfig()` 函数
   - 从 `config.json` 读取 `system_prompt`
   - 存储到 `CONFIG.systemPrompt` 变量

2. **发送到服务端** (`mcp_client.js`)
   - 调用 LLM API 时，将 `system_prompt` 包含在请求中
   - 字段名：`system_prompt`

3. **服务端处理** (`server/mcp_server.py`)
   - 服务端接收请求，提取 `system_prompt`
   - 将其作为 `system` 角色的消息添加到 messages 数组
   - 发送给 LLM API

## 提示词格式要求

### JSON 转义规则

在 JSON 中，以下字符需要转义：
- `\n` - 换行符
- `\"` - 双引号
- `\\` - 反斜杠
- `\t` - 制表符

### 示例：添加换行

```json
{
  "system_prompt": "第一段内容。\n\n第二段内容。\n\n第三段内容。"
}
```

### 示例：包含引号

```json
{
  "system_prompt": "你是一名 LegalMind，需要理解用户的\"法律问题\"。"
}
```

## 提示词变量替换

提示词中可以使用以下变量（由服务端替换）：
- `{current_user_input}` - 当前用户输入

示例：
```
理解用户问题 {current_user_input} 的核心需求
```

## 当前提示词内容概览

当前提示词包含以下主要部分：

1. **角色定义**
   - LegalMind 智能体的定位和能力

2. **思考流程**（6个步骤）
   - 理解用户需求
   - 分析资源可用性
   - 抽取必要参数
   - 判断提示词模板
   - 获取资源后处理
   - 最终输出要求

3. **输出格式要求**
   - 思考推理内容 + 分隔符 + JSON

4. **场景与返回数据结构**
   - 7种不同的响应类型
   - 每种类型的 JSON 结构

## 修改提示词的注意事项

1. **保持 JSON 格式正确**
   - 确保所有引号正确转义
   - 确保 JSON 语法有效

2. **保持输出格式一致**
   - 如果修改输出格式要求，需要确保客户端代码能正确解析
   - 分隔符 `--///--` 必须保留

3. **测试修改**
   - 修改后，测试各种场景
   - 确保 AI 能正确理解和响应

4. **备份原配置**
   - 修改前备份 `config.json`
   - 方便回滚

## 快速修改示例

### 示例1: 修改角色描述

```json
{
  "system_prompt": "你是一名专业的法律AI助手，专注于提供准确的法律咨询和文书生成服务。\n\n..."
}
```

### 示例2: 添加新的输出要求

在提示词中添加新的要求：
```json
{
  "system_prompt": "...\n\n【新增要求】\n- 所有回答必须引用具体的法律条文\n- 提供案例时需注明案例来源\n\n..."
}
```

### 示例3: 简化提示词

如果提示词太长，可以简化：
```json
{
  "system_prompt": "你是一名 LegalMind 助手。根据用户需求，使用可用资源（法律文书模板、法律法规、类案）完成任务。输出格式：思考内容 + --///-- + JSON。"
}
```

## 验证修改

修改后，可以通过以下方式验证：

1. **查看客户端日志**
   - 打开浏览器开发者工具
   - 查看控制台，确认 `System Prompt长度` 已更新

2. **测试功能**
   - 发送一条测试消息
   - 查看 AI 响应是否符合新的提示词要求

3. **检查服务端日志**
   - 查看 `mcp_server.log`
   - 确认 `system_prompt` 已正确加载

## 相关文件

- **配置文件**: `config.json`
- **客户端代码**: `mcp_client.js` (第137-249行)
- **服务端代码**: `server/mcp_server.py` (第1349-1402行)

## 总结

- ✅ 提示词定义在 `config.json` 的 `system_prompt` 字段
- ✅ 修改后需要刷新客户端或重启服务
- ✅ 注意 JSON 格式和转义字符
- ✅ 保持输出格式一致性
- ✅ 修改前备份原配置






