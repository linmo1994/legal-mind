# 工具资源调用问题分析和修复

## 问题描述

模型识别到需要调用工具，但客户端没有调用相关工具资源接口。

## 根本原因分析

### 问题1: 流式处理中未正确保留原始lawText

**位置**: `mcp_client.js` 第927-965行（流式处理）

**问题**:
- 当收到 `type: 7` 格式的数据时，代码会提取 `lawText`
- 如果 `lawText` 是 `invoke_tool_or_resource` 类型，代码会保留原始 `lawText`
- 但是，在构建 `fullContent` 时，如果 `lawText` 是JSON字符串，它会被直接拼接
- 最终返回给 `parseAndHandleResponse` 的格式是：`reasoningText + '\n--///--\n' + lawText`

**影响**:
- `parseAndHandleResponse` 会尝试从整个响应中提取JSON
- 如果响应格式是 `reasoningText + '\n--///--\n' + JSON字符串`，可能无法正确解析

### 问题2: parseAndHandleResponse 中的JSON解析逻辑

**位置**: `mcp_client.js` 第1620-1645行

**问题**:
- `parseAndHandleResponse` 会查找分隔符 `--///--`
- 然后尝试从分隔符后的内容中提取JSON
- 如果分隔符后的内容是 `invoke_tool_or_resource` 类型的JSON字符串，需要特殊处理

**当前逻辑**:
```javascript
const separatorIndex = response.indexOf(separator);
if (separatorIndex !== -1) {
  jsonContent = response.substring(separatorIndex + separator.length).trim();
  // 尝试解析JSON
  const jsonMatch = jsonContent.match(/\{[\s\S]*\}/);
  responseData = JSON.parse(jsonMatch[0]);
}
```

**问题**:
- 如果 `jsonContent` 是 `invoke_tool_or_resource` 类型的JSON字符串，解析后需要检查 `Type` 字段
- 但当前代码只检查 `responseData.type`，不检查 `lawQaText` 中的 `Type` 字段（对于非 `type: 7` 格式）

### 问题3: 响应格式不匹配

**客户端支持的格式**:

#### 格式1: 直接格式
```json
{
  "type": "invoke_tool_or_resource",
  "resource_uri": "legal://similar_cases",
  "parameters": {...}
}
```

#### 格式2: type: 7 嵌套格式
```json
{
  "type": 7,
  "data": {
    "lawQaText": "{\"Type\":\"invoke_tool_or_resource\",...}"
  }
}
```

#### 格式3: 分隔符格式（可能的问题格式）
```
思考内容
--///--
{"Type":"invoke_tool_or_resource","resource_uri":"...","parameters":{...}}
```

**问题**:
- 如果模型返回的是格式3，`parseAndHandleResponse` 会解析JSON，但可能无法正确识别 `Type` 字段

## 修复方案

### 修复1: 增强 parseAndHandleResponse 中的JSON解析逻辑

在 `parseAndHandleResponse` 中，解析JSON后，需要检查：
1. 如果 `responseData.type === 'invoke_tool_or_resource'`，直接处理
2. 如果 `responseData.type === 7` 且 `lawQaText` 包含 `Type: 'invoke_tool_or_resource'`，处理
3. 如果解析的JSON中包含 `Type: 'invoke_tool_or_resource'`（格式3），也需要处理

### 修复2: 增强流式处理中的检测逻辑

在流式处理中，如果检测到 `invoke_tool_or_resource` 类型，需要：
1. 保留原始 `lawText`（JSON字符串）
2. 在构建 `fullContent` 时，确保格式正确
3. 在 `parseAndHandleResponse` 中能够正确解析

## 具体修复代码

### 修复 parseAndHandleResponse 函数

需要在解析JSON后，检查所有可能的 `invoke_tool_or_resource` 格式：

```javascript
// 解析JSON后
if (responseData) {
  // 检查格式1: 直接格式
  if (responseData.type === 'invoke_tool_or_resource') {
    // 处理
  }
  
  // 检查格式2: type: 7 嵌套格式
  if (responseData.type === 7 && responseData.data && responseData.data.lawQaText) {
    const invokeInfo = parseInvokeToolOrResource(responseData.data.lawQaText);
    if (invokeInfo) {
      // 处理
    }
  }
  
  // 检查格式3: 分隔符后的JSON字符串中包含Type字段
  if (jsonContent && jsonContent.trim().startsWith('{')) {
    try {
      const parsedJson = JSON.parse(jsonContent);
      if (parsedJson.Type === 'invoke_tool_or_resource' || parsedJson.type === 'invoke_tool_or_resource') {
        // 处理格式3
        const invokeInfo = parseInvokeToolOrResource(jsonContent);
        if (invokeInfo) {
          // 处理
        }
      }
    } catch (e) {
      // JSON解析失败，忽略
    }
  }
}
```

## 调试建议

### 1. 检查浏览器控制台日志

查看是否有以下日志：
- `🔧 检测到invoke_tool_or_resource类型，准备调用工具/资源`
- `🔧 [流式] 检测到invoke_tool_or_resource类型`
- `JSON解析成功，type: ...`
- `响应前200字符: ...`

### 2. 检查模型返回的实际格式

在浏览器控制台中，查看：
- `响应前200字符:` 日志
- `完整响应:` 日志（如果有）
- `JSON解析成功，type:` 日志

### 3. 检查响应类型

确认模型返回的响应中：
- `type` 字段的值是什么？
- 如果是 `type: 7`，`data.lawQaText` 的内容是什么？
- 如果是分隔符格式，分隔符后的内容是什么？

## 结论

**不是SDK集成问题**。客户端已经实现了MCP协议的调用逻辑。

**可能的原因**:
1. 模型返回的响应格式不符合客户端预期（可能是格式3）
2. `parseAndHandleResponse` 中的JSON解析逻辑不完整，没有处理所有可能的格式
3. 流式处理中虽然检测到了，但最终解析时格式不匹配

**建议**:
1. 增强 `parseAndHandleResponse` 函数，支持所有可能的响应格式
2. 添加详细的日志，确认模型返回的实际格式
3. 根据实际情况调整解析逻辑






