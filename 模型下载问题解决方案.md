# paraphrase-multilingual-MiniLM-L12-v2 模型下载问题解决方案

## 问题分析

### 问题现象
系统在启动时尝试从 HuggingFace 下载模型，但出现连接超时错误：
```
Connection to huggingface.co timed out. (connect timeout=10)
```

### 根本原因

1. **网络连接问题**
   - 无法访问 `huggingface.co`（可能由于网络限制、防火墙或地理位置）
   - 连接超时时间过短（默认10秒）

2. **模型加载机制**
   - `SentenceTransformer` 在首次加载时会检查本地缓存
   - 如果缓存存在但不完整，仍会尝试从网络下载缺失文件
   - 即使本地有缓存，也会先尝试网络连接验证

3. **本地缓存状态**
   - ✅ 本地已有模型缓存：`~/.cache/huggingface/hub/models--sentence-transformers--paraphrase-multilingual-MiniLM-L12-v2/`
   - ❌ 但代码仍尝试网络连接，导致超时

## 解决方案

### 已实施的优化

已修改 `server/vector_service.py`，实现以下优化：

1. **优先使用本地缓存**
   - 检查本地缓存是否存在
   - 如果存在，设置 `HF_HUB_OFFLINE=1` 强制使用离线模式
   - 避免不必要的网络请求

2. **增加超时时间**
   - 设置 `HF_HUB_DOWNLOAD_TIMEOUT=300`（5分钟）
   - 如果网络可用，允许更长的下载时间

3. **改进错误处理**
   - 提供更详细的错误信息
   - 说明可能的原因和解决方案
   - 优雅降级到哈希向量备用方案

### 代码改进点

```python
# 检查本地缓存
if os.path.exists(cached_model_path):
    # 强制离线模式，使用本地缓存
    os.environ['HF_HUB_OFFLINE'] = '1'
    self.model = SentenceTransformer(model_name, device='cpu', cache_folder=cache_dir)
else:
    # 尝试在线下载
    self.model = SentenceTransformer(model_name, device='cpu', cache_folder=cache_dir)
```

## 验证步骤

1. **重启MCP服务器**：
   ```bash
   # 停止现有服务器
   pkill -f mcp_server.py
   
   # 启动服务器
   cd "/Users/kanglinlin/Documents/cursor/AI法官"
   python3 server/mcp_server.py > mcp_server.log 2>&1 &
   ```

2. **检查日志**：
   ```bash
   tail -f mcp_server.log | grep -E "(VectorService|模型|缓存)"
   ```

3. **预期输出**：
   ```
   [VectorService] ✅ 发现本地缓存模型: ...
   [VectorService] 使用本地缓存，跳过网络下载...
   [VectorService] ✅ 从本地缓存加载模型成功
   ```

## 如果问题仍然存在

### 方案1：使用HuggingFace镜像

如果网络无法访问 `huggingface.co`，可以使用镜像站点：

```bash
# 设置镜像环境变量
export HF_ENDPOINT=https://hf-mirror.com

# 或在代码中设置
os.environ['HF_ENDPOINT'] = 'https://hf-mirror.com'
```

### 方案2：使用代理

如果公司或网络环境需要代理：

```bash
export HTTP_PROXY=http://proxy.example.com:8080
export HTTPS_PROXY=http://proxy.example.com:8080
```

### 方案3：手动下载模型

如果以上方案都不可行，可以：

1. 在有网络的机器上下载模型
2. 将 `~/.cache/huggingface/hub/` 目录复制到目标机器
3. 确保目录权限正确

### 方案4：使用备用模型

如果无法使用 `paraphrase-multilingual-MiniLM-L12-v2`，可以考虑：

1. 使用更轻量级的模型
2. 使用本地训练的模型
3. 使用其他嵌入服务（如OpenAI Embeddings API）

## 当前状态

- ✅ 代码已优化，优先使用本地缓存
- ✅ 本地已有模型缓存（458MB）
- ✅ 错误处理已改进
- ⚠️ 需要重启服务器使更改生效

## 注意事项

1. **首次运行**：如果本地没有缓存，仍需要网络下载模型（约400MB）
2. **缓存完整性**：如果缓存不完整，可能需要补充下载缺失文件
3. **性能影响**：使用哈希向量备用方案会影响向量搜索准确性，但不影响系统基本功能

## 总结

问题的根本原因是：
- **网络无法访问 HuggingFace**，导致连接超时
- **代码没有优先使用本地缓存**，总是尝试网络连接

解决方案：
- ✅ **已优化代码**，优先使用本地缓存
- ✅ **增加超时时间**，允许更长的下载时间
- ✅ **改进错误处理**，提供更好的错误信息

**下一步**：重启MCP服务器，验证模型是否能从本地缓存正常加载。






