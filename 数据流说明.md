# 数据流说明文档

## 用户点击发送后的数据流

### 1. 客户端发送给代理服务的数据结构

**位置**: `mcp_client.js` -> `callLLM()` 函数

**请求URL**: `http://localhost:8000/api/llm/chat`

**请求方法**: POST

**请求体结构**:
```json
{
  "model": "deepseek-chat",
  "messages": [
    {
      "role": "system",
      "content": "系统提示词内容..."
    },
    {
      "role": "user",
      "content": "请根据以下数据分析和回答：\n\n{完整的requestData JSON}"
    }
  ],
  "temperature": 0.0,
  "max_tokens": 2048,
  "stream": true
}
```

**requestData 包含的内容**:
- `system_prompt`: 系统提示词
- `tools`: 工具列表（当前为空数组）
- `resources`: 资源列表（包含URI、描述、参数等）
- `session`: 会话信息（session_id、status、current_intent等）
- `conversation_history`: 对话历史
- `current_user_input`: 用户输入
- `config`: 配置信息

### 2. 代理服务接收的数据

**位置**: `server/mcp_server.py` -> `_handle_llm_proxy()` 函数

代理服务接收客户端发送的完整请求数据，包括：
- `model`: 模型名称
- `messages`: 消息数组
- `temperature`: 温度参数
- `max_tokens`: 最大token数
- `stream`: 是否流式输出

### 3. 代理服务发送给DeepSeek API的数据结构

**位置**: `server/mcp_server.py` -> `_handle_llm_proxy()` 函数

**API URL**: `https://api.deepseek.com/v1/chat/completions`

**请求方法**: POST

**请求体结构**:
```json
{
  "model": "deepseek-chat",
  "messages": [
    {
      "role": "system",
      "content": "系统提示词内容..."
    },
    {
      "role": "user",
      "content": "请根据以下数据分析和回答：\n\n{完整的requestData JSON}"
    }
  ],
  "temperature": 0.0,
  "max_tokens": 2048,
  "stream": true
}
```

**请求头**:
- `Content-Type`: `application/json`
- `Authorization`: `Bearer {api_key}`

### 4. 查看日志的位置

**客户端日志**（浏览器控制台）:
- 显示发送给代理服务的完整请求数据
- 包括消息详情和完整JSON

**服务端日志**（终端/控制台）:
- 显示接收到的客户端请求数据
- 显示发送给DeepSeek API的完整请求数据
- 包括消息内容预览和完整JSON

### 5. 数据流图

```
用户点击发送
    ↓
客户端构建requestData（包含系统提示词、资源、会话等）
    ↓
客户端构建messages数组（system + user消息）
    ↓
客户端发送POST请求到 /api/llm/chat
    ↓
代理服务接收请求数据
    ↓
代理服务构建llm_request（直接转发messages）
    ↓
代理服务发送POST请求到DeepSeek API
    ↓
DeepSeek API返回流式响应
    ↓
代理服务解析响应并格式化为type:7格式
    ↓
代理服务通过SSE返回给客户端
    ↓
客户端解析并显示
```

