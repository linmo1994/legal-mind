# 请求数据结构重构方案分析

## 当前结构

```javascript
{
  "system_prompt": "...",
  "tools": [...],
  "resources": [...],
  "prompts": [...],
  "session": {...},
  "conversation_history": [...],
  "current_user_input": "...",
  "file_ids": [...]
}
```

## 重构方案

### 方案1：统一能力字段 + 统一输入字段

```javascript
{
  "system_prompt": "...",
  "server_ability": {
    "tools": [...],
    "resources": [...],
    "prompts": [...]
  },
  "user_input": {
    "text": "用户输入的文本",
    "file_ids": ["file_123", "file_456"]
  },
  "session": {...},
  "conversation_history": [...]
}
```

### 方案2：更细化的结构

```javascript
{
  "system_prompt": "...",
  "server_ability": {
    "tools": [...],
    "resources": [...],
    "prompts": [...]
  },
  "user_input": {
    "text": "用户输入的文本",
    "files": [
      {
        "file_id": "file_123",
        "file_name": "document.pdf",
        "file_type": "application/pdf"
      }
    ]
  },
  "session": {...},
  "conversation_history": [...]
}
```

## 方案对比

### 优点

#### 方案1的优点：
1. ✅ **结构更清晰**：能力相关的内容统一在 `server_ability` 下
2. ✅ **输入统一**：用户输入（文本+文件）统一在 `user_input` 下
3. ✅ **语义明确**：字段名称更直观
4. ✅ **易于扩展**：未来添加新的能力类型或输入类型更容易

#### 方案2的优点：
1. ✅ 包含方案1的所有优点
2. ✅ **文件信息更完整**：不仅包含ID，还包含文件名、类型等元数据
3. ✅ **更灵活**：可以方便地添加文件相关操作

### 缺点

#### 方案1的缺点：
1. ⚠️ **需要修改服务端代码**：服务端需要适配新的数据结构
2. ⚠️ **向后兼容性**：需要考虑旧版本的兼容性

#### 方案2的缺点：
1. ⚠️ 包含方案1的所有缺点
2. ⚠️ **复杂度稍高**：文件信息需要额外获取和传递

## 推荐方案

**推荐使用方案1**，原因：
1. 结构清晰，语义明确
2. 实现相对简单
3. 满足当前需求
4. 易于扩展

## 实现建议

### 客户端修改（mcp_client.js）

```javascript
function buildRequestData(userInput, fileIds = []) {
  const requestData = {
    system_prompt: CONFIG.systemPrompt || '',
    
    // 统一的能力字段
    server_ability: {
      tools: (mcpTools || []).map(t => ({
        name: t.name,
        description: t.description || '',
        inputSchema: t.inputSchema || {}
      })),
      resources: (mcpResources || []).map(r => ({
        uri: r.uri,
        description: r.description,
        parameters: getResourceParameters(r.uri)
      })),
      prompts: (mcpPrompts || []).map(p => ({
        name: p.name,
        description: p.description || ''
      }))
    },
    
    // 统一的用户输入字段
    user_input: {
      text: userInput,
      file_ids: fileIds.length > 0 ? fileIds : undefined
    },
    
    session: {
      session_id: currentSession.sessionId,
      status: currentSession.status,
      current_intent: currentSession.currentIntent,
      collected_parameters: currentSession.collectedParameters,
      missing_parameters: currentSession.missingParameters,
      stage: currentSession.stage,
      context_cache: currentSession.contextCache,
      created_at: currentSession.createdAt,
      updated_at: new Date().toISOString()
    },
    
    conversation_history: currentSession.conversationHistory
  };
  
  return requestData;
}
```

### 服务端修改（server/mcp_server.py）

```python
def _handle_llm_proxy(self):
    # ...
    request_data = json.loads(post_data.decode('utf-8'))
    
    # 兼容新旧两种格式
    if 'server_ability' in request_data:
        # 新格式
        server_ability = request_data.get('server_ability', {})
        tools = server_ability.get('tools', [])
        resources = server_ability.get('resources', [])
        prompts = server_ability.get('prompts', [])
        
        user_input = request_data.get('user_input', {})
        current_user_input = user_input.get('text', '')
        file_ids = user_input.get('file_ids', [])
    else:
        # 旧格式（向后兼容）
        tools = request_data.get('tools', [])
        resources = request_data.get('resources', [])
        prompts = request_data.get('prompts', [])
        current_user_input = request_data.get('current_user_input', '')
        file_ids = request_data.get('file_ids', [])
    
    # 后续处理逻辑保持不变
    # ...
```

## 迁移策略

### 阶段1：双格式支持
- 客户端同时发送新旧两种格式
- 服务端优先使用新格式，如果没有则使用旧格式
- 确保向后兼容

### 阶段2：完全迁移
- 客户端只发送新格式
- 服务端移除旧格式支持
- 清理兼容代码

## 数据结构对比

### 当前结构
```javascript
{
  "system_prompt": "...",
  "tools": [...],           // 能力相关，分散
  "resources": [...],        // 能力相关，分散
  "prompts": [...],          // 能力相关，分散
  "current_user_input": "...", // 用户输入，分散
  "file_ids": [...],         // 用户输入，分散
  "session": {...},
  "conversation_history": [...]
}
```

### 重构后结构
```javascript
{
  "system_prompt": "...",
  "server_ability": {        // 能力相关，统一
    "tools": [...],
    "resources": [...],
    "prompts": [...]
  },
  "user_input": {            // 用户输入，统一
    "text": "...",
    "file_ids": [...]
  },
  "session": {...},
  "conversation_history": [...]
}
```

## 结论

重构方案**非常合理**，具有以下优势：

1. ✅ **结构更清晰**：相关数据聚合在一起
2. ✅ **语义更明确**：字段名称更直观
3. ✅ **易于维护**：逻辑分组更清晰
4. ✅ **易于扩展**：未来添加新字段更容易

建议采用**方案1**，在保持简单性的同时实现结构优化。
