# 工具/资源调用展示实现说明

## 功能概述

实现了在调用工具/资源时，客户端主动发起调用，并在界面上展示调用参数和返回结果的功能。

## 实现细节

### 1. 新增 UI 组件函数

#### `addToolInvocationMessage(type, name, parameters, status, result)`

**位置**: `mcp_client.js` 第1879行

**功能**:
- 创建工具/资源调用信息展示卡片
- 显示调用类型（资源/工具/提示词）
- 显示调用名称/URI
- 显示调用参数（JSON格式）
- 显示调用状态（正在调用/成功/失败）和结果

**参数**:
- `type`: 调用类型（'resource' | 'tool' | 'prompt'）
- `name`: 资源URI或工具名称
- `parameters`: 调用参数对象
- `status`: 调用状态（'calling' | 'success' | 'error'）
- `result`: 调用结果（可选）

**返回值**: 消息包装元素（用于后续更新）

#### `updateToolInvocationMessage(messageWrapper, status, result)`

**位置**: `mcp_client.js` 第1950行

**功能**:
- 更新已存在的工具/资源调用信息卡片
- 更新调用状态和结果

**参数**:
- `messageWrapper`: 消息包装元素
- `status`: 新的调用状态
- `result`: 调用结果

### 2. 增强资源调用函数

**位置**: `mcp_client.js` 第1970行（`handleResourceInvocation`）

**改进**:
1. **参数检查**: 在调用前检查 `resourceUri` 是否存在
2. **调用前展示**: 在调用前立即展示调用参数
3. **调用后更新**: 在调用成功后更新展示结果
4. **错误处理**: 在调用失败时更新展示错误信息

**代码流程**:
```javascript
// 1. 检查参数是否齐全
if (!resourceUri) {
  // 显示错误，不调用
  return;
}

// 2. 在调用前展示参数
const invocationMessage = addToolInvocationMessage('resource', resourceUri, parameters, 'calling');

// 3. 执行调用
const resourceResponse = await sendMCPRequest(...);

// 4. 更新展示结果
updateToolInvocationMessage(invocationMessage, 'success', resourceData);
```

### 3. 增强工具调用函数

**位置**: `mcp_client.js` 第2099行（`handleToolInvocation`）

**改进**:
1. **参数检查**: 在调用前检查 `toolName` 是否存在
2. **调用前展示**: 在调用前立即展示调用参数
3. **调用后更新**: 在调用成功后更新展示结果
4. **错误处理**: 在调用失败时更新展示错误信息

### 4. CSS 样式

**位置**: `mcp_client.css` 第845行

**新增样式类**:
- `.tool-invocation-message`: 工具/资源调用消息容器
- `.tool-invocation-card`: 调用信息卡片
- `.tool-invocation-name`: 调用名称/URI显示
- `.tool-invocation-params`: 调用参数显示
- `.tool-invocation-params-content`: 参数内容（代码格式）
- `.tool-invocation-result`: 调用结果显示
- `.tool-invocation-result-content`: 结果内容（代码格式）
- `.status-calling`: 正在调用状态样式
- `.status-error`: 错误状态样式

## 显示效果

### 调用前（正在调用）

```
📚 资源调用
┌─────────────────────────────────────┐
│ 资源URI: legal://similar_cases      │
│                                     │
│ 调用参数:                           │
│ {                                   │
│   "case_description": "合同纠纷"    │
│ }                                   │
│                                     │
│ 状态: 正在调用...                   │
└─────────────────────────────────────┘
```

### 调用后（成功）

```
📚 资源调用
┌─────────────────────────────────────┐
│ 资源URI: legal://similar_cases      │
│                                     │
│ 调用参数:                           │
│ {                                   │
│   "case_description": "合同纠纷"    │
│ }                                   │
│                                     │
│ 调用结果:                           │
│ [                                   │
│   {                                 │
│     "case_id": "2024-001",         │
│     "similarity": 0.95              │
│   }                                 │
│ ]                                   │
└─────────────────────────────────────┘
```

### 调用后（失败）

```
📚 资源调用
┌─────────────────────────────────────┐
│ 资源URI: legal://similar_cases      │
│                                     │
│ 调用参数:                           │
│ {                                   │
│   "case_description": "合同纠纷"    │
│ }                                   │
│                                     │
│ 调用失败: 资源不存在                │
└─────────────────────────────────────┘
```

## 自动调用逻辑

### 参数齐全检查

1. **资源调用**:
   - 检查 `resourceUri` 是否存在
   - 如果存在，自动发起调用
   - 如果不存在，显示错误信息，不调用

2. **工具调用**:
   - 检查 `toolName` 是否存在
   - 如果存在，自动发起调用
   - 如果不存在，显示错误信息，不调用

### 用户批准机制

- 如果 `autoApprove` 为 `true`（默认），自动批准并调用
- 如果 `autoApprove` 为 `false`，显示批准弹窗
- 用户拒绝时，更新调用信息为错误状态

## 数据流

```
检测到 invoke_tool_or_resource 类型
    ↓
解析参数 (resource_uri/tool_name/parameters)
    ↓
检查参数是否齐全
    ↓
是 → 展示调用参数（状态：正在调用）
    ↓
发起调用（sendMCPRequest）
    ↓
调用成功 → 更新展示结果（状态：成功）
调用失败 → 更新展示错误（状态：失败）
```

## 相关文件

- `mcp_client.js`: 客户端主文件
  - 第1879行: `addToolInvocationMessage` 函数
  - 第1950行: `updateToolInvocationMessage` 函数
  - 第1970行: `handleResourceInvocation` 函数（增强）
  - 第2099行: `handleToolInvocation` 函数（增强）
- `mcp_client.css`: 样式文件
  - 第845行: 工具/资源调用信息样式

## 测试建议

1. **测试资源调用**:
   - 发送请求，模型返回包含 `resource_uri` 和 `parameters` 的响应
   - 验证是否自动展示调用参数
   - 验证调用成功后是否展示结果

2. **测试工具调用**:
   - 发送请求，模型返回包含 `tool_name` 和 `parameters` 的响应
   - 验证是否自动展示调用参数
   - 验证调用成功后是否展示结果

3. **测试参数缺失**:
   - 发送请求，模型返回缺少必要参数的响应
   - 验证是否显示错误信息，不发起调用

4. **测试调用失败**:
   - 模拟资源/工具调用失败
   - 验证是否显示错误信息






